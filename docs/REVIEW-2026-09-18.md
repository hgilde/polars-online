# Review 2026-09-18 — every code file

**This file is the working state of the review. It is written to survive context
compaction: after a compaction, read §0 (protocol), §1 (which files are done), the
tail of §5 (log), and continue from the first unchecked file.**

Branch: `review-2026-09-18` (off `main` @ `8170558`, `v0.7.4`). Nothing is pushed yet. The
user's instruction, given mid-review on 2026-09-18: "When the review is complete fix
errors and then push" — so the branch is pushed **once, after phase 3's last batch has
been gated and committed**, and not before.

## 0. Protocol

**Scope.** Every code file in the repository, listed in §1 in review order:
`crates/*` (source, tests, examples), `crates/online-py`, `crates/online-cli`,
`python/polars_online`, `tests/`, `scripts/`, `.github/workflows`, and the three
manifests. Reviewed for: statistical / numerical errors, logic errors and bugs,
architecture flaws, performance, tests that cannot fail or pin a wrong oracle, CI
and packaging defects, and docstrings whose stated math disagrees with the code.

**Phase 1 — read.** Source files are read *in full*, not grepped. Tests and scripts
get a lighter pass aimed at wrong oracles, tolerances that hide errors, and tests that
pass on any code. After each file (or small group), before reading the next one:
record every candidate in §2 with enough quoted evidence (file:line, the exact
expression, the derivation) that a later context can reproduce it without re-reading
the file; tick the file in §1 with its finding IDs; append one line to §5.

**Phase 2 — reproduce and triage.** A candidate becomes a finding only once it is
reproduced by running code (the 2026-09-17 method) or re-derived independently with
a fresh eye. Anything that does not survive goes to §3 with the reason. Rank survivors
by consequence: *silent wrong output* > *crash* > *undocumented behaviour change* >
*lost guard* > *misleading message* > *cosmetic*. Group into batches (§4).

**Phase 3 — fix, per batch** (the user's rules from the 2026-09-12 round, which
still stand):
1. Write the batch's tests first. Run them once on the *current* build — every test
   must fail where its finding says. A test that passes on the old code is recorded
   in §3 and raised to the user; the code is not changed for it.
2. Make all the batch's fixes. Build once. Run the tests and the full gate once,
   unpiped: `bash scripts/gate.sh > "$S/gate-N.txt" 2>&1; echo "GATE EXIT $?" >> "$S/gate-N.txt"`
   and read `GATE EXIT` from the file — a backgrounded job's "exit 0" is the wrapper's.
3. `git branch --show-current` must print `review-2026-09-18`; commit with the finding
   IDs in the message. Push only after the last batch (the instruction above).
4. Decisions that are the user's (a behaviour change to shipped output, a schema
   bump, anything "needs a decision") are listed in §3 and asked, not made.

**Finding IDs.** `S` statistical/numerical · `B` logic bug · `A` architecture ·
`P` performance · `T` test defect · `X` CI/scripts/packaging · `D` docstring math
disagrees with code. Numbered per prefix in discovery order. Each entry: **Where**
(file:line, function) · **Claim** · **Evidence** (quoted) · **Consequence** ·
**Fix / test** · **Status** (`candidate` → `reproduced` | `refuted` | `known` →
`fixed <sha>` | `needs decision`).

**Standing constraints that bind the fixes.** No `unsafe` in `online-core`; f64
everywhere; `sklearn` never a dependency; `pandas` in tests only; no data files in
the repo; no `libm` outputs persisted in state; `n_eff` semantics per CLAUDE.md
rule 8 (under a `window`, the window's weight, everywhere); a zero-weight row is legal
including as the first row (rule 9); any state-layout change bumps `SCHEMA_VERSION`
(the user waived pre-1.0 loaders on 2026-09-14 — confirm before relying on it);
chunk invariance (rule 3) and out-of-sample prediction (rule 2) are non-negotiable;
no Agent tool or workflows unless the user asks; no platform-specific path without a
cfg-guarded fallback.

**Known open items, not re-raised.** PLAN §12: a zero-weight row on a *capped* gap
does not age the history (`EwCov::step_factors` refuses `lam = 0, w = 0`) — one row
wide, needs the user's decision. REVIEW-2026-09-17 A2: the Arrow input direction is
blocked on an ownership step, recorded not fixed.

**Build / test commands.** `source scripts/env.sh` (cargo/uv are not on PATH);
`cargo test --workspace`; `maturin develop --release -m crates/online-py/Cargo.toml`;
`uv run pytest -x`; the gate as above. Scratch: `$S` is this session's scratchpad.

## 1. File checklist (review order)

Tick with the finding IDs, or `—` for none. Line counts are at `8170558`.

### A. online-core foundations
- [x] crates/online-core/src/lib.rs (262) — —
- [x] crates/online-core/src/model.rs (313) — —
- [x] crates/online-core/src/humanfloat.rs (167) — —
- [x] crates/online-core/src/solve.rs (254) — —
- [x] crates/online-core/src/stats.rs (750) — S1, D1
- [x] crates/online-core/src/clock.rs (551) — B1; threads V1–V3
- [x] crates/online-core/src/gaps.rs (785) — —; threads V4, V6 (identities re-derived: `learn`, `miss`, `blend`, `truncated` all match their docs)
- [x] crates/online-core/src/window.rs (566) — —; thread V5 (pooling identity and boundary rule verified)
- [x] crates/online-core/src/drift.rs (231) — —

### B. online-core regressors
- [x] crates/online-core/src/ewridge.rs (3276) — B2, P1; thread V7 (solve paths, window, blend and gaps re-derived; tests read)
- [x] crates/online-core/src/rls.rs (641) — B3; thread V8 (Givens rotations, decay and prior re-derived)
- [x] crates/online-core/src/lasso.rs (1253) — B3 (sel fields), P1; (coordinate descent, selection, window read)
- [x] crates/online-core/src/robust.rs (1198) — S2, B3; thread V9 (Huber weights, quantile Newton step, both solves read)
- [x] crates/online-core/src/sgd.rs (1400) — B4 (gradients, links, scaler-including-row, AdaGrad, projection read)
- [x] crates/online-core/src/pa.rs (652) — B3 (three modes re-derived against Crammer et al.)
- [x] crates/online-core/src/ftrl.rs (857) — B3 (FTRL-proximal recursion and the C24 proximal sum re-derived)
- [x] crates/online-core/src/constraint.rs (548) — — (projection re-derived; KKT and Duchi tests read)
- [x] crates/online-core/src/conformal.rs (369) — — (ACI recursion and Acklam ppf checked)
- [x] crates/online-core/src/deco.rs (857) — B3 (Lemma 2.3 `u`, Woodbury block log-likelihood re-derived)

### C. online-core covariance, marginals, regimes, clustering
- [x] crates/online-core/src/ewcov.rs (4066) — B3 (restore), P1 (window snapshot); V4, V5, V6 closed clean (Welford step, blocked merge identity, precision solve, PCA, window view all re-derived; tests read)
- [x] crates/online-core/src/ewdiag.rs (367) — — (bit-identity with `EwCov` and `including` checked; `EwDiagWire` is the shape-check model for B3)
- [x] crates/online-core/src/ewlagcov.rs (356) — — (both legs on the pre-row mean; ring depth from the lags)
- [x] crates/online-core/src/rcov.rs (1557) — B3 (jitter ends, `Γ̂_h`, Parzen sum, MRC scale/bias, BNHLS bandwidth all re-derived against the papers)
- [x] crates/online-core/src/corrchange.rs (1354) — **S3** (D₃ gradient), B3 (Kolmogorov series, Bartlett LRV, CUSUM, permutation null read)
- [x] crates/online-core/src/marginal.rs (2183) — S4, B3, P1 (pair recursion, `cut`, serial factor, bins warm-up read)
- [x] crates/online-core/src/margbins.rs (934) — — (scale trick, West recurrence, stump gain, quantile edges checked)
- [x] crates/online-core/src/marglag.rs (241) — — (lag recursion matches `EwLagCov`; hold-not-decay rule read)
- [x] crates/online-core/src/ewclass.rs (1278) — S4, B3, P1 (QDA/LDA/NB scores and the factor cache read)
- [x] crates/online-core/src/hmm.rs (1019) — B3, minor (Hamilton filter, ξ-count learning, tvtp read)
- [x] crates/online-core/src/holt.rs (896) — B3 (weighted-mean recursion vs the longhand reference)
- [x] crates/online-core/src/kalman.rs (1574) — **B4** (NaN `obs_var` → silent no-learn), D2; V9 closed (filter, reversion, standardization read)
- [x] crates/online-core/src/bocpd.rs (1455) — B3; thread V10 (NIW/NIG predictives, Algorithm 1 indexing, tempering, prune/fold re-derived)
- [x] crates/online-core/src/seqtest.rs (588) — B3 (KT stakes, e-process, Ville test read)
- [x] crates/online-core/src/cluster/mod.rs (10) — —
- [x] crates/online-core/src/cluster/summary.rs (489) — — (Welford absorb/merge, SplitMix64 reference values)
- [x] crates/online-core/src/cluster/kmeans.rs (1729) — B3 (far cut derivation, seeding rules, split–merge read)
- [x] crates/online-core/src/cluster/micro.rs (1198) — B3 (DenStream `T_p`/`ξ` rule, linkage, unit-row decision read)

**§1.A–C complete: every `online-core` source file read in full.**

### D. online-core tests and examples
- [x] crates/online-core/tests/golden.rs (893) — T1
- [x] crates/online-core/tests/model_contract.rs (2030) — T2 (contract, recovery, predict-parity and zero-weight harnesses read in full)
- [x] crates/online-core/tests/state_encoding.rs (190) — B6, B7 (the rule it states, checked against every `skip_serializing_if` in the crate: 25 sites)
- [x] crates/online-core/examples/*.rs (7 files, 576) — — (benches and a signature printer; `Halflife(inf)` in `sgd_bench` is λ = 1, legal)

### E. online-polars source
- [x] crates/online-polars/src/lib.rs (80) — —
- [x] crates/online-polars/src/column.rs (332) — — (the bit-gather multiply re-derived: the eight products land on distinct bits 56–63, no carries; the run path overwrites whole bytes it fully covers)
- [x] crates/online-polars/src/rows.rs (147) — —
- [x] crates/online-polars/src/arrow.rs (496) — thread V12
- [x] crates/online-polars/src/atomic.rs (245) — —
- [x] crates/online-polars/src/pool.rs (116) — —
- [x] crates/online-polars/src/resid_window.rs (140) — — (snapshot `w·λ` matches `Moments::of`; direct-sum test)
- [x] crates/online-polars/src/summary.rs (554) — — (Welford with the shared reciprocal; `validate` consistency rules)
- [x] crates/online-polars/src/refresh.rs (704) — minor (BNHLS Definition 1 re-checked; ties by row order; state across feeds)
- [x] crates/online-polars/src/spec.rs (3047) — B4 grows; V7 closed, V11 partly (every numeric field's NaN/inf refusal audited; `Num` accepts the word `"nan"`, TOML accepts the literal `nan`)
- [x] crates/online-polars/src/bank.rs (4561) — — (five chunks; `assemble`'s index arithmetic for `Metric`/`Conformal`/`Quantile` re-derived against `output_index` and `ChunkOut::at`; averaging weights at `eta = inf` and `best = 0` checked)
- [x] crates/online-polars/src/stream.rs (3220) — minor ×2 (`run_instance` read line by line: pre-row reads of `sigma`/quantiles/metrics/conformal, the fold's zero-weight aging, the warmup-before-residual order, the label-delay score/replay pairing, the three scoring classes of `predict_chunk`)
- [x] crates/online-polars/src/runner.rs (1110) — minor (three-stage pipeline; channel shutdown paths; the closed-groups publish rule; parquet page encoding on polars' pool)

**§1.E complete: every `online-polars` source file read in full.**

### F. online-polars tests
- [x] crates/online-polars/tests/arrow_chunk.rs (305) — — (hand-built chunk vs frame, `equals_missing`; negative controls present)
- [x] crates/online-polars/tests/bank.rs (986) — T3 (chunk invariance, refused-chunk byte equality, held-block readers; one roll call is 16 of 21 kinds)
- [x] crates/online-polars/tests/chunk_plan.rs (847) — — (bit-for-bit `assert_same` across layouts, fragments, dtypes and the parallel threshold; `without_coef` strips one spec and compares every column)
- [x] crates/online-polars/tests/last_row.rs (352) — —
- [x] crates/online-polars/tests/runner.rs (564) — — (every format both ways against the bank's own numbers; failed runs leave no temporary)
- [x] crates/online-polars/tests/seqtest.rs (515) — — (comparison = column mode on the residual difference, by hand)
- [x] crates/online-polars/tests/spec_floats.rs (44) — —
- [x] crates/online-polars/tests/spec_inf.rs (121) — — (the natural home for B4's TOML-door tests; note under B4)
- [x] crates/online-polars/tests/state_portability.rs (133) — — (the cross-OS arm skips without `ONLINE_FOREIGN_STATE`, by design)
- [x] crates/online-polars/tests/summary.rs (844) — — (full oracle for summary/describe; the bit-flip fuzz never steps a loaded bank — note under B3)

**§1.F complete.**

### G. bindings and CLI
- [x] crates/online-py/src/lib.rs (699) — — (`PolarsAllocator` in place; every method takes its borrow with the `busy` message; the GIL is released around the bank calls; pickle via `load_bytes`)
- [x] crates/online-cli/src/main.rs (225) — minor (cosmetic)
- [x] crates/online-cli/tests/run.rs (364) — —

**§1.G complete.**

### H. Python package
- [x] python/polars_online/__init__.py (77) — —
- [x] python/polars_online/_kwargs.py (78) — —
- [x] python/polars_online/spec.py (307) — — (the conformal step, the averaging weights and the field grammar checked against `bank.rs`/`conformal.rs`)
- [x] python/polars_online/_spec.py (3689) — — (four chunks; every builder default checked against `build_bare`; the docstring recursions for ftrl's shrink ratio, seqtest's KT stake, marginal's Bartlett bracket and deco's Lemma 2.3 re-derived; `_json` refuses NaN by name, which narrows B4's doors)
- [ ] python/polars_online/_bank.py (1284)
- [ ] python/polars_online/_frame.py (1134)
- [ ] python/polars_online/prep.py (276)
- [ ] python/polars_online/gram.py (915)
- [ ] python/polars_online/corr.py (738)
- [ ] python/polars_online/sim.py (388)
- [ ] python/polars_online/eval.py (647)

### I. Python tests (oracles in full; the rest for tests that cannot fail)
- [ ] tests/conftest.py (132), tests/data.py (183)
- [ ] tests/reference.py (896)
- [ ] tests/reference_cluster.py (955)
- [ ] tests/test_second_opinion.py (2179)
- [ ] tests/test_*.py — remaining 78 files (32,134 lines), grouped by subject as read

### J. Scripts
- [ ] scripts/gate.sh (54), env.sh (27), coverage.sh (20), leakcheck.sh (189), mutants.sh (43)
- [ ] scripts/validate.py (234), benchmark.py (312), outputs_doc.py (133), io_source_semantics.py (278), make_example_parquet.py (30), scaling_bench.py (81), sklearn_comparison.py (349)
- [ ] scripts/clustering_experiments.py (1054), clustering_proto.py (1490), ogbt_experiments.py (451), ogbt_proto.py (721), regime_experiments.py (552) — research code, light pass

### K. CI and manifests
- [ ] .github/workflows/ci.yml (256), release.yml (388), benchmark.yml (86), leakcheck.yml (101), polars-canary.yml (111), dependabot.yml (22)
- [ ] Cargo.toml (53), pyproject.toml (157), rust-toolchain.toml (3)

## 2. Findings (candidates until §0 phase 2 says otherwise)

### B1. A backwards clock on a session change bypasses `on_clock_reset` — *lost guard*

**Reachability, from the bank read (2026-09-18): probably → §3.** `Spec::clock_cfg`
(`spec.rs:1776-1782`) requires `session_gap` whenever `session` is given, *except* under
`group_close = "session"`. So the only `ClockCfg` with a session column and
`session_gap: None` that the bank ever builds is a closing spec's — and there `process`
(`bank.rs:762-768`) splits the run at every session boundary and recreates the stream,
so the rows after the boundary never compare their clock with the rows before it.
`check_clock` (`bank.rs:2766-2777`) does run over the *whole* run on the old stream, so
an `Error` policy firing at the boundary would be a spurious refusal; the clamp is the
right answer for the one caller that reaches it. A direct `online-core` user can still
build the config, so the fix is a doc comment on `ClockState` (and perhaps a
`debug_assert`), not a behaviour change. Confirm against `clock.rs:207-247` and PLAN §3
in phase 2 before moving it.

**Where.** `crates/online-core/src/clock.rs:207-247`, `ClockState::advance`.
**Claim.** When `session_changed` and `cfg.session_gap` is `None`, the delta is
`raw.clamp(0.0, max_dclock)`: a negative raw delta becomes 0 silently. The `raw < 0.0`
arm that consults `on_clock_reset` (`Max` / `Zero` / `ResetState` / `Error`) is reached
only when the session did *not* change. So `on_clock_reset = "error"` — whose stated
purpose (line 59) is to make a mis-sorted chunk loud — never fires on a row whose
session id also changed, and `reset_state` never resets there.
**Evidence.** `if session_changed { match cfg.session_gap { …, None => { capped = raw >
cfg.max_dclock; raw.clamp(0.0, cfg.max_dclock) } } } else if raw < 0.0 { match
cfg.on_clock_reset { … } }`. The test `session_gap_overrides_delta` covers `Gap(7.5)`
("session gap wins") and `Reset`, never `None`.
**Consequence.** Interleaved or mis-sorted rows that happen to change session are
absorbed as a zero delta under every policy, including the one chosen to refuse them.
**Fix / test.** With `session_gap: None` a session change is an ordinary row for the
clock: fall through to the `raw < 0.0` policy. Test: `Error` policy, sessions 1 → 2 with
clock 10 → 4, expect `backwards == Some(-6.0)`; `ResetState` expects `reset`. Read PLAN
§3 for what is promised before changing.
**Status.** candidate.

### S1. The sign hit rate does not age on an excluded `y = 0` row — *inconsistent, low*

**Where.** `crates/online-core/src/stats.rs:283-313`, `SlotMetrics::update`.
**Claim.** A row skipped for a non-finite `pred`/`y` or `w <= 0` ages every estimate
(`joint.decay(lam)`, `hit_w *= lam`, lines 288-289). A finite row with `y == 0` under
`binary = false` is scored into `ic`/`r2` (`joint.update`, `mse`) but the hit-rate
branch (`if binary || y != 0.0`) is skipped whole, so `hits`/`hit_w` are neither scored
nor decayed. The hit rate then runs on a different clock from `ic` and `r2` whenever the
target is exactly zero (discrete or rounded targets).
**Fix / test.** `else { self.hit_w *= lam; }` on the excluded path. Test: 100 rows of
`(0.9, 1.0)` then 100 of `(0.9, 0.0)` at `lam = 0.9`: `hit_w` must shrink by `0.9^100`.
**Status.** candidate (low). Changes `hit_rate` only on streams with exact zeros — the
user's call.

### D1. `EwAutoCorr::get` reports `Some(0.0)` before any lagged pair when `lag ≥ 2` — *doc vs code, low*

**Where.** `crates/online-core/src/stats.rs:151-154` and `161-182`.
**Claim.** The doc says "`None` until a lagged pair has been seen", but `get` tests
`w > 0 && var > 0`, and `var > 0` after the second distinct observation; for `lag ≥ 2`
`cross` is still 0 then, so the accessor returns `Some(0.0)` for `lag − 1` rows in which
no pair exists.
**Fix / test.** Gate on `buf.len() == lag + 1`. Test at `lag = 3`: `None` after three
observations, `Some` after four.
**Status.** candidate (low).

### B2. `coef_prior` is ignored by the standardized solve through the origin — *silent wrong output*

**Where.** `crates/online-core/src/ewridge.rs:742-752` (the call) and `804-844`
(`solve_scaled_through_origin`).
**Claim.** `solve` has four branches. `ridge_decay` adds `ps·ridge·c0` to the right-hand
side (line 717-723); plain through the origin adds `ridge·c0` (734-740); the centred
solve, plain or standardized, adds `ridge·c0·s` on the scaled system (912-914). The
fourth — `add_intercept = false` with `standardize = true` — passes the bare `b` to
`solve_scaled_through_origin`, which scales, solves and unscales it and never reads
`self.cfg.coef_prior`. The prior is silently dropped: the fit shrinks toward zero.
**Evidence.** `Self::solve_scaled_through_origin(cov, &mut failures, &zidx, &b, kc, mr,
ridge)` — no `coef_prior` argument; the function body has no `c0`. `validate` (189-309)
checks the prior's shape and finiteness only; nothing refuses this combination. The
only prior-with-standardize test, `coef0_works_with_standardization` (1215), keeps the
intercept.
**Consequence.** A user asking for a warm-started, standardized, no-intercept fit gets
an un-warmed one with no error, and under an overwhelming ridge the coefficients are 0
where the docs promise `coef_prior`.
**Fix / test.** Pass `coef_prior` and `readers` in; add `ridge * c0[j][zidx[i]] * s[i]` to
`bsub[j*kk+i2]`, the same form as line 913. Test: `add_intercept = false, standardize =
true, ridge = 1e12, coef_prior = [[3, -2]]` → coefficients within `1e-3` of the prior;
fails today (lands at ~0). A second case at a small ridge against the plain
through-origin path with the same prior, which must agree to `1e-6` on well-scaled data.
**Status.** candidate.

### P1. A windowed `ew_ridge` builds its O(k²) snapshot on every row, even when the cadence discards it — *performance, low*

**Where.** `crates/online-core/src/ewridge.rs:986-996`, `step`.
**Claim.** `let snap = RidgeMoments { acc: self.acc.snapshot(lam), … }` runs before
`win.snaps.offer(t, || snap)`; the closure only moves an already-built value.
`Acc::snapshot` copies every Gram's `k×k` co-moments (`Moments::of` → `to_vec`) and the
`m×k` cross-moments. With `window_every = e` only one row in `e` is kept, so `e − 1` of
every `e` snapshots are built and dropped.
**Fix / test.** Build inside the closure: `offer(t, || RidgeMoments { acc:
self.acc.snapshot(lam), … })` — the borrow needs `acc`, `wsig`, `sig2` split from `win`
(they already are distinct fields). Bench at `k = 1000, window_every = 16`. Check the
other windowed models (`ewcov`, `lasso`, `marginal`) for the same pattern when read.
**Status.** candidate (low).

### S2. `robust` keeps raw cross-moments, so a level costs the fit `L²·ε` — *silent wrong output* (the N1/N2 defect, unfixed here)

**Where.** `crates/online-core/src/robust.rs:496-498` (the accumulator), `308-314` and
`329-332` (the plain solve), `409-412` (the standardized solve).
**Claim.** `r_j` is the EW mean of `z·target` — raw, with the intercept slot carrying
`ȳ` and each feature slot `E[x_i·y]`. The plain solve then reads the raw normal
equations `E[z zᵀ]β = r` (`a[i*k+jj] = self.cov[j].raw(i, jj)`), whose condition number
at a level `L` is `~L²/Var`; the standardized solve centres by subtraction,
`bsub = (b[i+off] − mean(i+off)·ybar)/s[i]`, two numbers of size `L²` leaving one of
size `Cov`. `ew_ridge`'s module doc states the consequence for exactly this form: "the
raw normal equations, or a right-hand side formed as `E[z y] − m ȳ`, lose `level²·ε`,
which at `1e8` is the whole fit". The 2026-09-12 round moved `ew_ridge` (N1) and `lasso`
(N2) to centred cross-moments (`crate::gaps::Cross`); `robust` was not moved, and its
docs do not say so.
**Evidence.** `robust.rs:497`: `*ri = a * *ri + bb * zi * target;` — no centring, no
`Cross`. Both `ewridge.rs` and `lasso.rs` carry `a_level_costs_the_fit_nothing` /
`a_level_costs_the_path_nothing` at `1e8`; `robust.rs` has no such test.
**Consequence.** Huber or quantile regression of a price on prices (the README's use
case) at a level of `1e8` returns slopes that are rounding noise, with no error and no
counted failure; at `1e4`–`1e6` the loss is partial and invisible.
**Fix / test.** Test first, from `ewridge.rs:2655`: Huber with `delta = 1e9` (least
squares) on the same stream at level 0 and `1e8`, slopes must agree to `1e-6` and
predictions minus the level to `1e-5`; expect it to fail today by orders of magnitude.
Then keep `r_j` centred as `Cross` does (mean of `z` over the target's *robust* weight,
`ȳ`, `c_j = E[(z − m)(y − ȳ)]`), and solve the centred system with the intercept
eliminated as `EwRidge::solve_centred` does; the quantile nudge enters `c_j` as
`nudge·(z − m)/aged` with `ȳ` untouched (a nudge has no target). Schema bump: `r`'s
meaning changes (`SCHEMA_VERSION` 11, `MIN` 11 under the pre-1.0 waiver — the user's
call).
**Status.** candidate — needs the reproduction before anything else.

### S3. `corrchange`'s delta-method gradient has the wrong powers of σ, so the monitor statistic depends on the data's units — *silent wrong output*

**Where.** `crates/online-core/src/corrchange.rs:386-391`, `long_run_sd`, the `d3`
array; the same expression in the test `the_statistic_is_its_definition` (864-868).
**Claim.** `ρ = σ_xy·(σ_x²)^{-1/2}·(σ_y²)^{-1/2}`, so the gradient with respect to
`(σ_x², σ_y², σ_xy)` is `(−½·σ_xy/(σ_x³·σ_y), −½·σ_xy/(σ_x·σ_y³), 1/(σ_x·σ_y))`.
The code has `−0.5·sxy·sy/(sx·sx·sx)` and `−0.5·sxy·sx/(sy·sy·sy)`: the first entry is
multiplied by `σ_y²`, the second by `σ_x²`. Units: `∂ρ/∂σ_x²` must be `1/x²`; the code's
is `y²/x²`. Only at `σ_x = σ_y = 1` do the two coincide.
**Evidence.** Under `y → c·y`, `σ_xy → c·σ_xy`, `σ_y → c·σ_y`: the correct entry is
unchanged (ρ is scale-free), the code's scales by `c²`, and its contribution to `D̂²`
(`f·Σ̂·f'`, with `Σ̂₀₀` the long-run variance of `x²`, unchanged by `y`'s scale) by `c⁴`.
So `Q = max_j (j/√T)|ρ̂_j − ρ̂_T|/D̂` moves with the units of `y`, which a correlation
test cannot do. Every test feeds `Normals::pair` — unit variance both sides — so the
paper's size and power tables are reproduced exactly where the defect vanishes; the
"definition" test carries the same formula.
**Consequence.** On returns in basis points against returns in percent (`σ` ratio 100),
`D̂` is wrong by orders of magnitude and the monitor flags nothing or everything, with
no error. Users on unit-scale data are unaffected.
**Fix / test.** `d3 = [-0.5*sxy/(sx*sx*sx*sy), -0.5*sxy/(sx*sy*sy*sy), 1.0/(sx*sy)]`.
Tests: (1) invariance — the same stream with `y` multiplied by 100 must give `Q` equal
to `1e-9`; fails today. (2) the gradient against a central finite difference of
`corr_of` in each of the three moments. (3) keep the size test but on
`(x, 100·y)`. Also fix the longhand in the existing test, which mirrors the bug.
**Status.** candidate — reproduce with (1) first.

### S4. A windowed `marginal` reports a rounding crumb as `n_eff` where its pairs report 0 — *cosmetic, low*

**Where.** `crates/online-core/src/marginal.rs:586-599`, `n_eff` and `target_weight`.
**Claim.** Both return `(w − f·old).max(0.0)` with no `EMPTY_FRACTION` test, where
`cut` (line 574) and every other windowed model treat a remainder under
`1e-12·w` as empty. After a gap longer than the window the model's `n_eff` is a
positive crumb (`~W·1e-16`) while every `Pair::n_eff` is exactly 0.
**Fix / test.** Apply the same rule; test: a gap of twice the window, `n_eff() == 0.0`
exactly and `target_weight(t) == 0.0`. **Same in `ewclass.rs:293-304`**, `EwClass::n_eff`.
**Status.** candidate (low).

### B3. `Rls::restore` and `Robust::restore` accept a state of any shape — *crash instead of an error, low*

**Where.** `crates/online-core/src/rls.rs:284-297`, `robust.rs:565-578`; also
`lasso.rs:703-722` for `sel_err`/`sel_w`/`sel_idx`.
**Claim.** `EwRidge::restore` and `Lasso::restore` refuse a state whose accumulators do
not match `cfg` (`has_shape` → `StateError::Invalid`). `Rls::restore` only resizes the
scratch buffers; `Robust::restore` only `zbuf`. A state whose `r` is shorter than
`k_total²`, or whose `cov` has fewer entries than `n_targets`, restores and panics on the
first `step` (`self.r[row + i]`, `self.cov[j]`). `Lasso` checks `acc` but not
`sel_idx`, whose entries index `lasso_path` in `lam_selected`.
**Consequence.** A hand-edited JSON state (`Bank::save_json_string` exists for that)
or a truncated file crashes the process instead of returning the error the bank
reports for every other model.
**Fix / test.** A `has_shape` per model, checked in `restore`; test: a state with
`r` truncated by one element restores as `Err(StateError::Invalid)`. Keep a list of
every model found without the check as the read continues.
**Status.** candidate (low). Models without a shape check so far: `rls`, `robust`,
`pa` (`beta` vs `n_targets`), `ftrl` (`n`/`zz`; and `prox` is `#[serde(default)]`, so a
state without it loads with `prox = []` and `self.prox[j][i]` panics on the first
step); `lasso` (selection fields); `deco` (`rho`/`rho_bar` lengths, and `blocks` is
read from the state — an index `>= n_features` panics in `block_sums`); `ew_cov`
(`EwCovModel::restore` checks nothing: `cov.k()` vs `cfg.n_features`, `mahal_q.len()`
vs `mahal_quantiles.len()`, a `Pca` whose `loadings.len() != r·k`); `rcov` (`raw`,
`gamma`, `pre_sum` and the two `StridedRv`s against `k`, `ring_for()` and the strides —
`restore` only trims the tail); `corrchange` (`ring` row widths, `diag.k()`);
`marginal` (every per-pair vector against `p·t`, `min_periods` against `t`, and the
boxed `lag`/`bins` against the cfg); `ew_class` (`classes.len()` vs `n_classes`, each
`k()`); `hmm` (`states.len()` vs `k`, `p.len()`, `a.len()` vs `k²`); `holt` (`level`,
`trend`, `seen`, `w_level`, `w_trend` vs `n_targets` — `restore` checks `since` alone,
and `w_level`/`w_trend` are `#[serde(default)]`, so a state without them loads empty and
`self.w_level[j]` panics); `bocpd` (`runs.len() == logjoint.len()`, each run's
`mean.len() == d` and `m2.len()` per emission); `seqtest` (the four vectors vs
`n_targets`); `kmeans` (`clusters`, `rows`, `batch`, `far`, `far_rows` all `k` long once
seeded, `mw` vs `p`); `micro` (`mw` vs `p`, every `s.c.len()`, `n_clusters` consistent
with the labels). `sgd` is covered by `TryFrom<SgdV3>`, `kalman` by
`TryFrom<KalmanV3>`, `EwDiag` by `EwDiagWire` — the pattern to copy. **Every
`online-core` model except `sgd`, `kalman` and (partly) `ew_ridge`/`lasso` is on this
list: one finding, one fix pattern, ~17 `restore` sites.**

Reproduction (phase 2): `crates/online-polars/tests/summary.rs:791-844` already flips
bits across a saved bank and asserts "refused or loaded, never a panic", but its
`survives` closure only calls the readers (`summary`, `describe`, `last_row`,
`groups`, `save_bytes`) — a flipped *length header* of a model vector loads today and
would first fail inside `step`. Extending `survives` with one `fit_predict` row on
the loaded bank is the test; expect a panic today on some length flips, an error
after the fix.

### B4. `SgdCfg::validate` lets a NaN `clip_gradient` through to `f64::clamp`, which panics — *crash, low*

**Reachable from the TOML door and from hand-written JSON** (spec.rs read, refined
after `_spec.py`): `Num::from_word` accepts `"nan"` (`spec.rs:258`), so a JSON spec can
carry `"clip_gradient": "nan"`, and TOML's float grammar has a `nan` literal, so any
plain `f64` key can too. The Python builders are not a door: `_json`
(`_spec.py:49-50`) refuses a NaN by parameter name before anything is serialized. So
the CLI and a Rust caller are exposed; a Python caller is not. `Spec::validate` refuses NaN
for every numeric key it checks (audited: `lam`, `halflife`, `max_dclock`, `session_gap`,
`min_periods`, `label_delay`, drift, conformal, quantiles, `average_eta`, the `kalman`,
`huber`, `quantile`, `ftrl`, `lasso`, `rls`, `ew_ridge`, `ew_cov`, `ew_class`, `kmeans`,
`micro` keys) **but not** `sgd`'s `clip_gradient`, `power`, `l2`, `eps`, `quantile` (the
loss level) and `coef_sum`, nor `pa`'s `coef_sum`, nor the `coef_prior` entries of
`ew_ridge`/`rls` (V8) — those are left to the core `validate`s. Phase 2 checks each
against `SgdCfg::validate`/`PaCfg::validate`; whatever the core does not refuse is fixed
there (one door for the bank, the CLI and a Rust caller).

**Core `validate`s printed (2026-09-18).** `SgdCfg::validate` refuses NaN for
`learning_rate`, `huber delta` and the quantile level, but its `l2 < 0.0`, `eps < 0.0`,
`clip_gradient <= 0.0` and `power < 0.0` tests all pass a NaN (a comparison with NaN is
false). NaN `l2` poisons every coefficient on the first update; NaN `eps` makes the
insensitive tube empty, so the loss is silently `|r|`. `PaCfg::validate` is clean
(`is_nan` on both). `RlsCfg::validate` (`rls.rs:76-79`) checks `coef_prior`'s shape only,
where `EwRidgeCfg` (`ewridge.rs:294`) also requires finite entries — a NaN prior enters
`u_0 = √ridge·prior` and the QR state never recovers (V8 confirmed; `rls` only).
Batch: `!(v >= 0.0)` / `!(v > 0.0)` forms for the four `sgd` fields, the finiteness
check copied to `rls`, `Constraint::validate` re-read for `lo`/`hi`/`sum` NaN, one
test per field on the old build. The TOML-door tests belong in
`crates/online-polars/tests/spec_inf.rs`, which already has the `MEANS_SOMETHING` /
`MEANS_NOTHING` tables and a `nan_is_refused_where_inf_means_something` sweep: add a
`NAN_IS_NO_SETTING` table with `sgd`'s `clip_gradient`, `power`, `l2`, `eps` and
`rls`'s `coef_prior = [[nan, 0.0]]`.

**Where.** `crates/online-core/src/sgd.rs:159-161` (the check), `583`, `587`, `594`,
`599` (the clamps); `178-182` for `InvScaling { power }`.
**Claim.** `if self.clip_gradient <= 0.0` is false for NaN, so the config validates;
`(d * zi * weight + l2 * *b).clamp(-clip, clip)` then panics on the first learned row
("min is NaN"). The validator's own comment for Huber's `delta` (line 163) states the
hazard: "NaN too: `f64::clamp` panics on a NaN bound." `clip_gradient` is serialized
`with = "f64_or_tag"`, so a JSON spec can carry `"nan"`. `InvScaling { power: NaN }`
passes `power < 0.0` and gives `lr = NaN`, NaN coefficients for the life of the state.
**Fix / test.** `if !(self.clip_gradient > 0.0)` (NaN-safe) and `if !(power >= 0.0)`;
tests in `cfg_validation_rejects_each_bad_field`: `clip_gradient = NaN` and
`power = NaN` both refused. Whether the spec layer already blocks them is V7's
question; the crate should not rely on it.

**Same hole in `kalman.rs:103-136`, and there it is silent.** `halflife` (`h <= 0.0`
is false for NaN → `q = σ²·(ln2/NaN)² = NaN` → `P` NaN → NaN predictions), `p0`
(`p0 <= 0.0` false → `P₀ = NaN`), `q` entries (`v < 0.0` false), and **`obs_var`**:
`is_some_and(|v| v <= 0.0)` is false for NaN, so `s_inn = zᵀPz + NaN/w = NaN`, the
guard `s_inn > 0.0 && s_inn.is_finite()` on line 604 is false on **every row**, and the
filter never updates `beta` or `P` — it predicts the prior for the life of the stream
with no error and no counted failure. `revert_halflife` is NaN-checked (S32 reached it);
these four were missed. The same `!(v > 0.0)` form fixes all of them; tests per field.
**Status.** candidate (low for `sgd`; `kalman`'s `obs_var` case is a silent no-learn and
should be ranked with it).

### D2. `Kalman::pred_var` is the filtered variance, not the predictive one — *doc vs code, low*

**Where.** `crates/online-core/src/kalman.rs:285-326`.
**Claim.** The doc calls it "predictive variance of the *last* prediction: `zᵀP_j z +
R_j`". `P` as stored is the post-update (filtered) covariance; the prediction the next
`step` makes uses `Φ P Φ + Q·d` (lines 517 and 562-572), so the one-step-ahead
predictive variance is `zᵀ(ΦPΦ + Q·d)z + R`, and `pred_var` omits `Q·d` and the
transition. Under halflife-derived `q = σ²(ln 2/h)²` the shortfall is about
`√(q/R)` relatively (≈ 0.7 % at `h = 100`); under an explicit large `q` it is not small.
**Fix / test.** Either add `Q·d_clock` (and `Φ`) for a caller-supplied `d_clock`, or
say "filtered" in the doc and in `docs/OUTPUTS.md`. The user's call; the doc change
alone is safe.
**Status.** candidate (low).

### T1. The golden file pins no through-origin fit — *test coverage, low*

`crates/online-core/tests/golden.rs` runs every `ew_ridge`, `robust`, `rls`, `kalman`,
`lasso`, `sgd`, `pa` and `ftrl` signature with `add_intercept: true`. The
`add_intercept: false` branches — including the one B2 is in — and `kalman` with
`standardize: false` move no golden number, so a regression there is invisible to
`cargo test`. Fix (with B2's batch): one `ew_ridge_origin_std` signature over the same
stream; optionally `robust` and `kalman` through the origin.

### T2. Contract harness gaps — *test coverage, low*

- `model_contract.rs:168` `KEEPS_LAGS` names `rcov`, `corrchange`, `ew_cov`; `marginal`
  also keeps a lag ring (`marglag.rs`) but is probed with `lags: Vec::new()`, so its
  `clear_lags` arm is exercised nowhere in Rust.
- `model_contract.rs:1918-1945` `hmm_recovers_from_bounded_extremes` steps a `twin`
  through all ~30 500 rows and then discards it (`let _ = twin;`): a full-covariance
  two-state filter run for nothing, on every `cargo test`.
- `model_contract.rs:584-595` `every_state_kind_is_distinct_and_named` checks a literal
  list of 15 kinds for duplicates (5 kinds missing: `deco`, `rcov`, `hmm`,
  `corrchange`, `bocpd`); the assertion is on the list, not the states. Cosmetic.

### B5. `kmeans`: a far summary absorbed while `far_cut = ∞` sets a cluster radius to ∞ — *verify; crash-adjacent, low–medium*

`cluster/kmeans.rs` (line refs in the phase-2 note): `refresh_far_cut()` leaves
`far_cut = ∞` until some cluster has `RADIUS_ROWS` rows with `r2 > 0`, and `is_far` is
off while it is ∞. But `try_seed` marks rows far by the *buffer's* cut, so a far summary
can hold `f.n > 0` right after seeding. `winsorize_radii()` then forms
`c.r2 = (c.n·r2 + f.n·far_cut)/(c.n + f.n)` with `far_cut = ∞`, so `c.r2 = ∞`, and the
next `refresh_far_cut` sums an infinite radius (typical radius and cut ∞ or NaN; every
row is then "far" or none is). Reachable with a small `split_merge_every` or
`update_every` right after `warm_rows`. Phase 2: build with `k = 3`, `warm_rows = 6`,
`update_every = 1`, `split_merge_every = 1`, feed 3 tight blobs plus one outlier in
the seed buffer, assert every `r2` and `far_cut` finite after each checkpoint.
Fix, if it reproduces: skip the far pool in `winsorize_radii` while `far_cut` is not
finite (or clamp `f.n·far_cut` to the buffer cut used at seeding).

### B6. `EwCovModel` has two `skip_serializing_if` fields, so a windowed `ew_cov` without lags does not round-trip compactly — *lost guard (the crate's own rule), low*

`ewcov.rs:1255-1261`: `lag: Option<EwLagCov>` and `win: Option<Windowed>` are both
`skip_serializing_if = "Option::is_none"`, `lag` first. `state_encoding.rs` states the
rule: "at most one field may skip, and it must be last", because the compact msgpack
encoding is positional. A state with a window and no lags writes `[…, win]`, and the
reader decodes `win` into `lag`'s slot. Exactly the shape that broke `marginal` (E66)
and was fixed there by leaving `win` as the only skip. Test: `EwCovModel` with
`window: Some(50.0)`, `window_every: Some(1)`, `lags: vec![]`, ten rows,
`rmp_serde::to_vec` → `from_slice` must succeed and continue identically (fails
today, expected). Fix: drop the skip on `lag` (a `nil` in the named file; `default`
loads both ways, so no loader is owed — whether rule 5 wants a `SCHEMA_VERSION` bump
for the byte change is the user's call). Production impact: none if only the named
encoding is ever written (checked below in the log).

### B7. `window::Moments::q` is skipped when `None` and is not the last field — *lost guard (same rule), low*

`window.rs:283-289`: `Moments { w, q: Option<f64> (skip if none), m, c }`. A snapshot
without a Kish sum encodes compactly as `[w, m, c]` and decodes `m` into `q`. `q` is
`Some` only where the accumulator tracks `Q` (`marginal`), which is why
`state_encoding.rs`'s marginal probe passes; a windowed `ew_cov` or `ew_class`
snapshot has `q: None`. Same test shape as B6 (either model with a window, compact
round-trip); fix: move `q` after `c`, or write it unconditionally. Named encoding
unaffected (maps are order-free).

### T3. `online-polars` test roll calls that cannot fail for a new kind — *test coverage, low*

- `crates/online-polars/tests/bank.rs:622-673`
  (`only_models_that_predict_a_target_have_residual_fields`): the doc says "one spec
  per `ModelKind`; a new kind that lands on the wrong side of this line shows up here",
  but the list is a literal of 16 kinds (no `deco`, `rcov`, `hmm`, `corrchange`,
  `bocpd`) and the final assertion only checks the 16 are distinct. Fix: assert
  `seen.len() == ModelKind::KINDS.len()` (and add the five specs), so the test fails
  when a kind is added and not listed — the same defect as T2's third bullet.

### Minor, unnumbered (no action proposed unless asked)

- `refresh.rs:263-291`: the group key (a `String`) is cloned twice per input row
  (`last_time.insert(key.clone(), t)` and `states.entry(key.clone())`), two allocations
  a tick on the hot path of a tick-stream operator; a `get_mut` before the `insert` and
  an `entry` on a borrowed key would remove both.
- `spec.rs:918-944`: the `marginal` variant has six `skip_serializing_if` keys ahead of
  three unskipped ones — the shape B6/B7 forbid — but a spec is only ever written in the
  named encoding (JSON, TOML, `to_vec_named`), so it is not a positional-decode risk.
- `arrow.rs:168-171` (`ArrowChunk::key`) returns the first non-numeric form under a
  name; with one integer column serving as both `session` and `group` that is the text
  form, and `group_close = "monotone"` then orders "10" before "9" and refuses a sorted
  chunk. One-line fix: prefer `I64`/`U64` over `Str` in `key`. (V12.)
- `stream.rs:1321-1341`: `StreamState` ends in four `skip_serializing_if` fields
  (`pending`, `score_pred`, `resid_win`, `last_session`) — the B6/B7 shape again, but a
  `StreamState` only ever travels inside a `BankFile`, which is written named
  (`bank.rs:3133`), so not a positional-decode risk. Same class as the spec's
  `marginal` variant above.
- `stream.rs:574`: an `ew_cov` spec's `min_periods` is silently floored at 2
  (`.max(2.0)`) — a variance needs two rows, but a spec saying `min_periods = 0` gets 2
  without a word. Doc or refuse; cosmetic.
- `runner.rs:477-485`: `check_parent` guards `save_state` and `closed_groups` before a
  row is read, but not `output`; an unwritable output directory is found by the writer
  thread after the bank has learned the first chunk (the run fails and nothing is
  published, so only a caller-owned bank handed to `run` is left one chunk ahead).
  One more `check_parent("writing output", ..)` under `!cfg.no_output()`.
- `online-cli/src/main.rs:212-217`: under `--no-output` the closing line reads
  `wrote N rows (M chunks) to ` with an empty path. Cosmetic: say "(no output)" there.

- `hmm.rs:556-559`: a learned row whose densities fail every jitter rung counts a
  `solve_failure` and returns before the states and transition counts are aged by
  `lam`; `n_eff` was already advanced. Every other "unscored" path here ages. Rare
  (all five rungs must fail).
- `hmm.rs:396-406`: the `full` shape refactorizes every state on every row, `O(K·d³)`,
  where `ew_class` caches; under `learn = true` every state moves every row so a cache
  would only help states at `p_s = 0`, and under `learn = false` it would help every
  row. Performance, not correctness.

### Threads to pull in later files

- **V11** — `window_every` without `window`: five Cfgs (`ew_ridge`, `lasso`, `ew_cov`,
  `ew_class`, `marginal`) end in two `skip_serializing_if` fields, `window` then
  `window_every`; compactly, `window: None, window_every: Some(n)` decodes `n` into
  `window`. Safe only while every path refuses a cadence without a window — confirm the
  crate `validate`s and `spec.rs`; if one lets it through it joins B6's batch.
- **V12** — `ArrowChunk::key` (`arrow.rs:168-171`) returns the first non-`F64` form under
  a name, and `wanted()` (`arrow.rs:257-293`) pushes `session` (text) before `group`
  (key), so an integer column read as both roles is bucketed — and, under
  `group_close = "monotone"`, ordered — as text. Confirm in `bank.rs` whether one column
  may serve both roles; if so the fix is to prefer the integer form in `key`.
- **V7 → closed at the spec door** (`spec.rs:2059-2072, 2775, 2883, 2922`, `check_solve_every`):
  `min_periods`, `ridge` entries, `solve_every`, `cd_tol` and `quantile_eps` are all
  refused when NaN before a model is built. The core `validate`s stay lax for a direct
  Rust caller; that is B4's list, not a new finding.
- **V10 → closed** (`bank.rs:378-393`): a finite hazard `≤ 1` in the hazard column is
  refused naming the row; null/non-finite falls back to the spec's `hazard`, as
  documented.
- **V11 → partly closed**: `spec.rs:2370-2372` (`ew_cov`) and `2572-2574` (`ew_class`)
  refuse `window_every` without `window`; the `ew_ridge`, `lasso` and `marginal` arms
  have **no window checks at all** (`spec.rs:2845-2889, 2907-3043, 2622-2638`), so
  those three rely on the core `validate` — grep result in the log.
- **V11 → closed**: every core `validate` refuses `window_every` without `window`
  (`ewridge.rs:222`, `lasso.rs:101`, `marginal.rs:141`, `ewcov.rs:1085`,
  `ewclass.rs:133`), so the two trailing skips are safe: `window: Some, window_every:
  None` writes `[…, w]` and the missing tail defaults. Not a finding.
- **V8 → closed as part of B4**: `rls` does not check `coef_prior` finiteness; `ew_ridge`
  does.
- **V12 → minor** (bank read): `group_indices` (`bank.rs:987`) and
  `group_key_is_integer` (`bank.rs:1050`) both go through `ArrowChunk::key`, so a column
  read as both `session` and `group` is bucketed by text; the partition is the same
  (decimal text is injective) and the only consequence is that `group_close =
  "monotone"` then orders the keys bytewise and **refuses** a chunk with keys 9, 10
  ("group 10 after group 9"). Loud, not silent, and a strange spec. Listed under Minor.

- **V1** (from `clock.rs:27-43`): `Decay::Halflife(0.0).factor(0.0)` is `exp2(-(0/0))`
  = NaN, and a negative `Lam` gives NaN under a fractional delta. Confirm `spec.rs` /
  `_spec.py` reject `halflife <= 0` and `lam ∉ (0, 1]`; if not, the first row of a
  stream poisons the state.
- **V2** (from `clock.rs:81`, `227-231`): "`max_dclock` … required when a clock column
  is used". With `OnClockReset::Max` and `max_dclock = ∞`, a backwards clock gives
  `d = ∞` → factor 0 → total forget. Confirm the spec enforces the requirement.
- **V3** (from `clock.rs:250-283`): a `reset` decided on a *skipped* row (`accept =
  false`; `ResetState` or `SessionGap::Reset`) is returned with `accepted: false`.
  Confirm `stream.rs` still resets the model, or the reset is lost with the row.
- **V4** (from `gaps.rs:346-347`): the no-split rule for a zero-weight row relies on
  `EwCov::update(z, lam, 0.0)` ageing the Gram by the same steps as `EwCov::skip(z,
  lam)`, "to the bit". Confirm in `ewcov.rs`.
- **V5** (from `window.rs:295-301, 340-371`): `Moments::of` stores `cov.comoments()` and
  `truncated` applies the pooling identity to it as a *mean-form* centred co-moment;
  confirm `comoments()` and `set_moments` are mean-form, not sum-form.
- **V6** (from `gaps.rs:349-360`): a Gram split under `gram_block_rows` clones a Gram
  that may hold an unflushed block and calls `skip` on the clone. Confirm `clone` +
  `skip` and the original's `update` treat the held block the same way.
- **V7** (from `ewridge.rs:189-309`): `EwRidgeCfg::validate` does not check
  `min_periods` (NaN never predicts: `n_eff >= NaN` is false), `ridge` entries (negative
  → indefinite system; NaN → every rung fails, silently keeping stale coefficients), or
  `solve_every` (NaN never fires the clock clause). Confirm `spec.rs` rejects them on
  every entry path, the CLI's TOML included. Same for `LassoCfg` (`cd_tol`,
  `max_cd_iters`, `select_halflife`, `min_periods`) and `RobustCfg` (`min_periods`,
  `solve_every`; `quantile_eps = NaN` passes `<= 0.0` and is then masked by `max`).
- **V8** (from `rls.rs:69-82`): `RlsCfg::validate` checks `coef_prior`'s shape but not
  its finiteness, where `EwRidgeCfg` checks both; a NaN prior gives `u = NaN` and NaN
  coefficients for the life of the state. Confirm the spec layer catches it.
- **V9** (from S2): check `kalman`, `sgd`, `ftrl`, `pa` for the same raw-cross-moment /
  raw-normal-equation form at a level. C10/C11/C13 fixed their *centring without an
  intercept*; whether their precision at a level was addressed is a separate question.
  **`sgd`, `pa`, `ftrl`: not applicable** — gradient steps and projections, no normal
  equations, no accumulated cross-moments. **`kalman`: closed, not affected** — it
  standardizes through a Welford `EwDiag` by default, and the unstandardized filter
  forms no second-moment sums (rank-1 updates of `P` and `β`; the innovation
  `y − zᵀβ` has the data's own resolution, not `L²·ε`). **V9 is closed: `robust`
  (S2) is the only model with the defect.**
- **V10** (from `bocpd.rs:553-563`): `hazard_of` says an unusable row hazard (`<= 1`
  or non-finite) "is refused by the plumbing before it reaches here"; if it is not,
  `read` returns NaN and the row counts as a `solve_failure`, which is visible but
  mislabelled. Confirm in `stream.rs`/`bank.rs` where the hazard column is read.
- **P1 applies to `lasso` too** (`lasso.rs:643-653`, the same eager snapshot), **and
  to `ew_cov`** (`ewcov.rs:1697-1703`: `let snap = Moments::of(&self.cov, lam);
  win.snaps.offer(t, || snap)`) — the model where `k` is largest (the docs cite
  `k = 400`), so the wasted `k²` copy per non-snapshot row is the costliest instance.
  **And `marginal`** (`marginal.rs:896-911`: seven vector clones of size `p·t` per row)
  **and `ew_class`** (`ewclass.rs:571-583`: one `Moments::of` per class per row).
- **V4 closed — no defect.** `ewcov.rs:698-739` and `761-772`: `update(z, lam, 0.0)`
  takes `a = 1, b = 0`, so `c` and `m` are untouched and `commit_scalars` does
  `w_sum = lam·w_sum`, `q = lam²·q`, `prior_scale *= lam`, `precision_scale *= 1`;
  `decay(lam)` does the same three products and does not touch `precision_scale`.
  Identical to the bit, as `gaps.rs` relies on. `test a_zero_weight_row_is_still_a_decay`
  pins `w_sum`/`q_sum` (not the moments, which cannot move under either).
- **V5 closed — no defect.** `EwCov.c` is the mean-form centred co-moment (struct doc,
  and `update`'s `*cj = a * *cj + ab_di * dj`); `Moments::of` copies it and
  `truncated` applies the mean-form pooling identity; `set_moments` takes the same
  form. Consistent throughout.
- **V6 closed — no defect.** `Pending` is a plain field, so `grams[g].clone()` carries
  the held rows and `w_open`; the clone's `skip` buffers a zero-weight row and the
  original's `update` buffers the weighted one, and each later `flush` merges its own
  list against the `w_open` both share. Correct by construction.

<!-- findings-end -->

## 3. Checked, and not a finding — or the user's decision

<!-- refuted-end -->

## 4. Batches

<!-- batches-end -->

## 5. Log

- 2026-09-18: branch created; protocol and checklist written; reading starts with §1.A.
- 2026-09-18: §1.A lib, model, humanfloat, solve, stats, clock read → B1, S1, D1; threads V1–V3. Next: gaps, window, drift, ewridge.
- 2026-09-18: gaps, window, drift read → no findings; threads V4–V6. §1.A complete. Reads must stay ≤ ~1100 lines (25k-token cap). Next: ewridge in three chunks.
- 2026-09-18: ewridge read → B2 (prior dropped in standardized through-origin solve), P1; thread V7. Next: rls, lasso, robust.
- 2026-09-18: rls, lasso, robust read → S2 (robust raw cross-moments at a level — the N1/N2 defect), B3; threads V8, V9. Next: sgd (two chunks), pa, ftrl.
- 2026-09-18: sgd, pa, ftrl read → B4; B3 grows (pa, ftrl); V9 closed for these three. Next: constraint, conformal, deco; then ewcov in four chunks.
- 2026-09-18: constraint, conformal, deco read → B3 grows (deco). **§1.B complete.** Next: ewcov (four chunks; settles V4–V6), then ewdiag, ewlagcov, rcov.
- 2026-09-18: ewcov read → V4/V5/V6 closed clean; B3 and P1 grow. State doc committed (milestone 1, `1abab63`). Next: ewdiag, ewlagcov, rcov (two chunks).
- 2026-09-18: ewdiag, ewlagcov, rcov read → no new findings; B3 grows (rcov). Next: corrchange (two chunks), marginal (two chunks).
- 2026-09-18: corrchange, marginal read → **S3** (corrchange D₃ gradient wrong by σ² — scale-dependent statistic), S4; B3, P1 grow. Next: margbins, marglag, ewclass (two chunks).
- 2026-09-18: margbins, marglag, ewclass read → no new findings; S4/B3/P1 grow (ew_class). Next: hmm, holt, kalman (two chunks).
- 2026-09-18: hmm, holt, kalman read → B4 grows (kalman NaN `obs_var` = silent no-learn), D2, minor notes; B3 grows; V9 closed. Next: bocpd (two chunks), seqtest; then cluster/*.
- 2026-09-18: bocpd, seqtest read → B3 grows; thread V10. Next: cluster/{mod, summary, kmeans (two chunks), micro} — the end of §1.C.
- 2026-09-18: cluster/* read → B3 grows. **§1.A–C complete** (36 source files). State doc committed (milestone 2). Next: §1.D — golden, model_contract (two chunks), state_encoding, examples.
- 2026-09-18: §1.D read → T1, T2, B5 (carried from kmeans), B6, B7. The `skip_serializing_if` rule was checked at all 25 sites: five Cfgs pair `window`/`window_every` as two trailing skips (safe only while `window_every` without `window` is refused — thread V11), `ewridge`/`lasso`/`marginal` states keep `win` last (`ewridge`'s trailing `zbuf` is `serde(skip)`, fine), `bocpd.solve_failures` last; `EwCovModel` (B6) and `Moments` (B7) break it. **§1.D complete.** Next: §1.E lib, column, rows, arrow, atomic, pool, resid_window (one round), then summary, refresh, spec (three chunks), bank (five), stream (three), runner.
- 2026-09-18: online-polars foundations (7 files) clean; summary, refresh clean (one P-minor); spec.rs read in three chunks (validation audit above). bank.rs 1-920 read: extraction, monotone check, session-close segmentation, comparison targets — nothing numbered yet, V10 closed there. Next: bank.rs 920-4561 (four chunks), then stream.rs.
- 2026-09-18: bank.rs 920-3680 read (group indexing, monotone/session close, the two-phase fit/predict, save/load, coef and output indexes): no new numbered finding; B1 is likely unreachable except where its behaviour is right (note under B1); B4 grows (`sgd` l2/eps NaN, `rls` prior finiteness); V8, V11 closed; V12 → minor. Next: bank.rs 3680-4561, stream.rs (three chunks), runner.rs.
- 2026-09-18: bank.rs complete (clean). stream.rs 1-2040 read: `AnyModel` dispatch, every `build_bare` arm against its spec defaults, `StreamState`/`Stream::restore` (shape checks where the core's are missing: model count, `resid_win.fits`, `last_row.to_chunk`, `summary.validate`), `check_clock`. Two minors. Next: stream.rs 2040-3220 (`process_chunk`, `predict_chunk`), runner.rs.
- 2026-09-18: stream.rs 2040-3220 and runner.rs read: clean (one minor each). **§1.E complete** (13 files, 14 752 lines). Running total: B1–B7, S1–S4, D1–D2, P1, T1–T2, minors; threads open: V1–V3 (clock semantics in PLAN §3; V2 answered by `spec.rs:1739` — `max_dclock` is required with a clock column), V9 closed earlier. Next: §1.F online-polars tests (arrow_chunk, bank, chunk_plan, last_row; then runner, seqtest, spec_floats, spec_inf, state_portability, summary), milestone-3 commit after them.
- 2026-09-18: online-polars tests arrow_chunk, bank, chunk_plan, last_row read → T3. Next: runner, seqtest, spec_floats, spec_inf, state_portability, summary; then milestone-3 commit and §1.G.
- 2026-09-18: online-polars tests complete (§1.F): no new finding; B3 gains a reproduction (extend the bit-flip fuzz with a `fit_predict`), B4 its TOML-door test home. Next: §1.G online-py `lib.rs`, online-cli `main.rs` + `tests/run.rs`; milestone-3 commit with them.
- 2026-09-18: §1.G read (py binding, CLI, CLI tests): clean, one cosmetic. **Every Rust file in the workspace is now read** (§1.A–G). Milestone-3 commit next, then §1.H Python package: `__init__`, `_kwargs`, `spec`, `_spec` (four chunks), then `_bank`, `_frame`, `prep`, `gram`, `corr`, `sim`, `eval`.
- 2026-09-18: §1.H begun: `__init__`, `_kwargs`, `spec`, `_spec` (four chunks) clean; B4's doors narrowed. Next: `_bank.py` (two chunks), `_frame.py` (two), `prep.py`; then `gram`, `corr`, `sim`, `eval`.
<!-- log-end -->
