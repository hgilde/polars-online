# Release readiness and API stability

Status as of 2026-08-31: **proposed.** Two questions — what is left before
this repo goes public, and what would make its API safe to promise.

---

# Part 1 — Before going public

Ordered by "would embarrass us if a stranger found it first".

## Already done

Apache-2.0 `LICENSE`, `CONTRIBUTING.md`, `SECURITY.md`, `CHANGELOG.md`, issue
and PR templates, an outward-facing README, `release.yml` with wheels for six
platforms plus an sdist and PyPI trusted publishing, the name `polars-online`
verified free, and a test suite that is the repo's strongest argument (230 Rust
+ ~680 pytest, with golden and hardening layers).

## R1 — Least-privilege workflow permissions *(do first; security)*

None of the four workflows sets a top-level `permissions:` block, so every job
inherits the repository default. `release.yml` correctly narrows two jobs
(`contents: write`, `id-token: write`) but the rest run wider than they need.

On a **public** repo this matters more than on a private one: any fork's PR can
trigger workflows, and a token with more scope than the job needs is the
standard supply-chain foothold.

Fix: `permissions: {}` at the top of every workflow, granting per job only what
that job uses (`contents: read` for checkout, and the two `release.yml` grants
that already exist).

## R2 — Pin actions to commit SHAs

All nine third-party actions are pinned to mutable tags (`@v5`, `@v2`,
`@release/v1`). A tag can be repointed by whoever owns the action. For a
private repo this is a low risk; for a public one publishing signed artifacts
to PyPI it is the thing supply-chain audits look for first.

Fix: pin to full SHAs with the version in a trailing comment, and add
Dependabot (`.github/dependabot.yml`, `package-ecosystem: github-actions`) so
they still get updated — pinning without automation just means stale actions.

## R3 — Decide whether the Rust crates are published

No crate sets `publish`, so `cargo publish` would happily push `online-core`,
`online-polars`, `online-cli` and `online-py` to crates.io. That is four more
public APIs to maintain, and `online-py` in particular is meaningless outside
the wheel.

Two coherent answers:
- **Python only** (recommended for 0.1.0): add `publish = false` to all four,
  and say in the README that the Rust crates are an implementation detail.
- **Publish `online-core`** deliberately — it is genuinely reusable (no Polars
  dependency, no `unsafe`, exhaustively tested) — and mark the other three
  `publish = false`.

Doing nothing is the bad option, because it leaves the choice to whoever next
runs `cargo publish`.

## R4 — Repository settings *(needs the web UI; not scriptable from here)*

- Branch protection on `main`: require CI to pass, no force-push. Right now a
  bad push cannot be caught by anything.
- Description and topics (`polars`, `online-learning`, `streaming`,
  `regression`, `rust`) — this is how anyone finds it.
- Enable private vulnerability reporting, which `SECURITY.md` already tells
  people to use.

## R5 — Green CI on all three platforms first

Do not make it public with a red badge. As of now Windows and Linux fixes are
in flight; macOS was green on the first run. Flip only after a full run passes.

## R6 — A history scan for anything that should not be public

100 commits, none of which should contain a secret, but "should" is not
"checked". `gitleaks detect` or a `git log -p | grep -iE 'password|token|
secret|-----BEGIN'` pass before flipping, since making a repo public
retroactively exposes every commit, not just the tip.

---

# Part 2 — Keeping the API stable

## What the API actually is

Bigger than `__all__` suggests. Four surfaces, in descending order of how
easily we could break someone:

**1. Output field names — the largest and least-guarded.**

```
pred_y__r0.000001@h100    resid_y__r0.5@h100    absresid_q0.95_y0    n_eff@h500
```

Users write `out["m"].struct.field("pred_y")`. These strings are generated by
`format!` over ridge values, halflife suffixes, feature-set labels, quantile
levels and target names. **Every one of them is API**, and today exactly *one*
spec shape is pinned (`test_exact_field_names_for_a_grid_spec`, 52 fields).
Adding an output, renaming a suffix, or changing how a float renders silently
breaks downstream code, and the failure surfaces as a `StructFieldNotFound` in
someone else's pipeline.

**2. Twelve spec constructors, ~180 keyword parameters.** All keyword-only
(good — positional order is not API), but every *name* is. Nothing pins them.

**3. Defaults, which are API in the worst way.** `solve_every = halflife/50`,
`standardize` true for lasso and false for ridge, `average_eta = 1.0`,
`clip_gradient = 1e3`. Changing one of these does not raise — it silently
changes users' numbers, which is worse than breaking them. `docs/VALIDATION.md`
justifies them and `test_validation_doc.py` pins that they are still the
measured optimum, but nothing pins the *values* as a contract.

**4. State files and the TOML config.** Already the best-guarded: versioned
msgpack, `SCHEMA_VERSION` plus a bank `format_version`, a frozen v1 fixture,
and a documented rule (hard rule 5) to keep a previous-version loader. This is
the model the rest should follow.

## S — The mechanism: one API snapshot test

The concrete proposal, and the highest-value item in this document.

A single test that renders the entire public surface to text and compares it to
a checked-in file:

- every public symbol in `polars_online` and `polars_online.spec`;
- every spec constructor's **full signature including default values**;
- the expression-namespace method list;
- `output_fields()` for a canonical matrix of ~12 spec shapes — each model,
  plus grids, feature sets, multi-target, and every `emit_*` combination;
- `SCHEMA_VERSION` and the bank `format_version`.

```
tests/test_api_surface.py   ->  compares against  tests/api_surface.txt
```

Regenerate with `UPDATE_API_SURFACE=1 pytest tests/test_api_surface.py`.

Why this shape rather than more unit tests: **it turns every API change into a
reviewable diff.** A rename shows up as `- pred_y__r1e-06 / + pred_y__r0.000001`
in the PR, where a human decides whether it is intended and whether it needs a
major version. Accidental changes fail; deliberate ones are visible and
deliberate. It is the same trick `test_golden_pipeline.py` plays on the numbers,
applied to the names.

It also subsumes the current single-shape field-name test, and would have
caught the E23 declared-vs-realized divergence from the other direction.

## Policy to write down

Short, in the README and CONTRIBUTING:

- **Stable:** everything in `__all__`, the `spec.*` constructors and their
  keyword names, the `.online` namespace, output field names, the state file
  format, the TOML config keys.
- **Not stable:** anything underscore-prefixed, the Rust crates (pending R3),
  the exact numeric output (it depends on the polars version and the platform,
  within the 1e-12 tolerance `test_golden_pipeline.py` pins).
- **Changing a default is a breaking change**, because it changes results
  silently. It needs a minor bump pre-1.0, a major bump after, and a CHANGELOG
  entry saying what moved and by how much.
- Pre-1.0, the **minor** version carries breaking changes; `~=0.1.0` is the
  pin for users who want stability.

## What not to do

- **Do not `__getattr__`-guard the submodules.** `polars_online._spec` is
  importable, and someone will reach into it; that is what the underscore is
  for. Enforcement costs more than the honesty is worth.
- **Do not freeze the API before 0.1.0 ships.** The snapshot test is for
  detecting change, not preventing it. The point is to know, not to be stuck —
  and right now, before there are any users, is exactly when changing names is
  cheap.
- **Do not add `cargo semver-checks`** unless R3 says the crates are published.
  It is the right tool for a published Rust API and pure overhead otherwise.

## Suggested order

1. **R1, R2, R3** — small, mechanical, and all three are things a reviewer of a
   public repo checks in the first minute.
2. **The API snapshot test** — before 0.1.0, while renames are still free.
3. **The policy paragraphs** in README and CONTRIBUTING.
4. **R6** scan, **R5** green CI, then **R4** settings, then flip visibility.
