# Code review, 2026-09-12

A read-through of the library looking for bugs and inconsistencies. Nothing
here was executed: the container this was written from has no network, so
every finding is from reading the code, and each one names the test that
would confirm or refute it -- first the test that fails today, then, where
an independent implementation exists, the library that gives a second
opinion. Items are ordered by how much they can change a number the user
reads.

Six passes so far. The first read the contract-critical path; the second
the other coefficient models that share its machinery and the atomic
writer; the third `robust` and `ew_class`, which turned three findings
into patterns; the fourth added the tests -- a **Test** paragraph on every
finding, and the "Second opinions" section that gives the exact parameter
mappings to `sklearn`, `statsmodels`, `scipy` and `filterpy`, so the
tests are drop-in beside `tests/test_river.py`. The fifth read `sgd.rs`
and `ewcov.rs` in full: V8 closed the wrong way (pattern B now has four
instances and one reference), `ew_cov` joined patterns A and D, and the
accumulator's own `blend` turned out to reintroduce the cancellation its
module doc opens by explaining away. The sixth read `marginal.rs` in full
and re-read `window.rs` and `EwRidge::blend_toward_long_run` for that
cancellation, and found it in both: it is a fifth pattern (E), it is in
the shared truncation every windowed model calls, and one identity fixes
all four sites. From this pass on, every **Test** paragraph leads with the
library comparison and the from-scratch oracle comes second.

## Method

Depth first on the contract-critical path -- the model trait, the clock, the
solve, the windowing identity, the workhorse model, the stream that drives
them, and the IO plugin -- then the models that reuse that machinery, then
breadth by search on the rest. The plan below is the whole library; the
status column is honest about what was and was not read.

## Plan

| # | Section | Files | Status |
|---|---------|-------|--------|
| 1 | Model contract and versioning | `online-core/src/{model,lib}.rs` | read in full (pass 1) |
| 2 | Clock and decay | `online-core/src/clock.rs` | read in full (pass 1) |
| 3 | Dense solves | `online-core/src/solve.rs` | read in full (pass 1) |
| 4 | Hard-cutoff windows | `online-core/src/window.rs` | read in full (pass 1; re-read pass 6 for pattern E) |
| 5 | EW-ridge (the workhorse) | `online-core/src/ewridge.rs` | read in full (pass 1; `blend_toward_long_run` re-read pass 6) |
| 6 | The other coefficient models | `lasso.rs`, `rls.rs`, `kalman.rs` | read in full (pass 2) |
| 6b | | `robust.rs` | read in full (pass 3) |
| 6c | | `sgd.rs` | read in full (pass 5) |
| 6d | | `ftrl.rs`, `pa.rs`, `holt.rs` | **open** |
| 7 | Accumulators and statistics | `ewcov.rs` (160 KB) | read in full (pass 5) |
| 7b | | `ewdiag.rs`, `ewlagcov.rs`, `marglag.rs`, `stats.rs` | **open**; `EwDiag::including` is what `sgd` standardizes against (C13); the two lag rings are what C18's snapshot question is about |
| 8 | Unsupervised and regime models | `ewclass.rs` | read in full (pass 3) |
| 8b | | `marginal.rs` | read in full (pass 6) |
| 8c | | `cluster/`, `hmm.rs`, `bocpd.rs`, `corrchange.rs`, `deco.rs`, `margbins.rs`, `rcov.rs`, `seqtest.rs`, `drift.rs`, `conformal.rs`, `constraint.rs` | **open** |
| 9 | Stream: clock schedule, label delay, diagnostics, scoring | `online-polars/src/stream.rs`, `rows.rs` | read in full (pass 1) |
| 10 | Bank, spec, refresh, runner, summary, pool | `online-polars/src/{bank,spec,refresh,runner,summary,pool}.rs` | `Spec::validate`'s grid and feature-set checks located by search (V2 partly closed); **open** (`bank.rs` 195 KB, `spec.rs` 112 KB) |
| 10b | Atomic state writes | `online-polars/src/atomic.rs` | read in full (pass 2) |
| 11 | CLI | `online-cli/src/main.rs` | **open** |
| 12 | Python bindings | `online-py/src/lib.rs`, `python/polars_online/_polars_online.pyi` | `closed_groups` signature by search; **open** |
| 13 | IO plugin and frame namespaces | `python/polars_online/_frame.py` | read in full (pass 1) |
| 14 | Python bank, specs, kwargs, expression plugin | `_bank.py`, `_spec.py`, `_kwargs.py`, `_expr.py`, `_runner.py`, `spec.py` | **open** |
| 15 | Python analysis helpers | `corr.py`, `eval.py`, `gram.py`, `prep.py`, `sim.py` | **open** |
| 16 | Tests and docs consistency | `tests/`, `crates/*/tests/`, `docs/` | `test_river.py` read in full (pass 4); the rest only where a finding names a test |

Next pass, in order: `spec.rs`'s `validate` in full, then `bank.rs`'s
`extract` and `assemble`, then `_bank.py`; then `ewlagcov.rs` and
`marglag.rs` together, to settle whether the lag rings can be
snapshotted (C18) or the two `window` + `lags` refusals should match.

## Patterns

Five findings recur across models. A fix should land in every instance,
and a test written for one should be run against the others.

- **A. A live accumulator read inside a windowed path.** The windowed
  view is built and then one read goes to `self.…` instead: `ewridge`'s
  standardized solve (C1), `ew_class`'s `full`-shape factor (C12),
  `ew_cov`'s `mahal` and PCA refresh (C14), `marginal`'s lag ring (C18).
  Grep for `self.cov`, `self.classes`, `self.lag` inside any function that
  has a `view`, a `boundary()` or a `cut` in scope -- and for any `&self`
  method called from `read` or `pair`, since those take the view as an
  argument and the method does not.
- **B. Centring without an intercept.** With `standardize` on and
  `add_intercept = false`, `lasso` (C8), `kalman` (C10), `robust` (C11)
  and `sgd` (C13) centre the features anyway; `ewridge` alone scales by
  the raw second moment. Four wrong, one right; the fix is the same line
  in each.
- **C. A zero-weight row treated differently from a null target.**
  `lasso`'s selection has no `0/0` guard (C7); `kalman` (S9) and `robust`
  (S13) decay the per-target residual weight on a null target but not on
  a zero weight. `ewridge` gets both right and is the reference; so do
  `sgd`, whose two arms fall through to the same `continue`, and
  `marginal`, whose `learn` takes a `lam = 0` zero-weight row to
  `W_t = 0` on purpose.
- **D. Which `n_eff` a windowed model reports.** `ewridge` reports the
  in-window weight; `lasso` (C9) and `ew_class` (S14) report the whole
  history while fitting on the window; `ew_cov` (S15) emits the in-window
  weight and its accessor returns the whole history; `marginal` (S17)
  does the exact reverse of `ew_cov` -- accessor windowed, emitted field
  live. Five models, three conventions, one option. Hard rule 8 should
  say which.
- **E. Re-centring through the raw second moment.** `E[x²] − m²` at a
  large offset destroys the variance, which is why `EwCov` went to
  Welford updates; four places still compute it: `TargetMoments::blend`
  (C16), `EwRidge::blend_toward_long_run` (C16, V9 confirmed),
  `window::truncated` (C17) and `marginal::cut` (C17). Each is a
  weight-respecting combination -- or its inverse -- of two sets of
  moments, and the centred identity for both directions is one line:
  `W·C = W_a·C_a + W_b·C_b + (W_a·W_b/W)·(m_a − m_b)(m_a − m_b)ᵀ`.
  Solve it for `C` to blend, for `C_b` to truncate. Nothing in it is
  larger than a centred moment or a difference of means.

## Second opinions

`tests/test_river.py` is the model: `pytest.importorskip`, an **exact**
tier where the two libraries implement the same published recursion, and
a **statistical** tier where they differ by design. The proposal is one
new file, `tests/test_second_opinion.py`, with `sklearn`, `statsmodels`,
`scipy`, `filterpy` and `pandas` as optional dev dependencies, and the
mappings below -- each worked out from the objective the model states in
its module doc, so the *exact* tier really is exact. `docs/TESTING.md`
section B is where they belong, as T-S1..T-S12.

The one identity everything rests on: our accumulators are **mean-form**
(`S = Σ w zz' / W`, `r = Σ w z y / W`, `W = Σ w`), so a penalty `ridge`
on `S` is a penalty `ridge · W` on the sum-scale normal equations every
library solves. With a halflife, the row weights are `w_t · lam^(age_t)`
with `lam = 0.5^(1/halflife)` on a row-count clock; with a `window`, the
rows older than it are dropped before the weights are formed.

- **T-S1, `ew_ridge`, `standardize = false` -- `sklearn.linear_model.Ridge`.**
  `Ridge(alpha = ridge * W, fit_intercept = add_intercept).fit(X, y,
  sample_weight = w)`. sklearn centres `X` and `y` by the weighted means
  and leaves the intercept unpenalized, which is what `D = I minus the
  intercept slot` does; the two solutions are the same to rounding
  (`atol 1e-9` on standard-normal features). Exact, every row where our
  `solve_every` fired (`max_rows_between_solves = 1` to compare every
  row). `statsmodels.WLS` is the same check with `ridge = 0`.
- **T-S2, `ew_ridge`, `standardize = true` -- `Ridge` on standardized X.**
  `Xs = (X - mean_w(X)) / sd_w(X)` with the weighted moments;
  `Ridge(alpha = ridge * W, fit_intercept = True).fit(Xs, y,
  sample_weight = w)`; `coef / sd_w` are our slopes and `intercept -
  Σ coef·mean_w/sd_w` our intercept. Exact. Without an intercept, `Xs =
  X / sqrt(E_w[x²])` and `fit_intercept = False` -- the no-intercept
  branch scales by the raw second moment, and this is the check that
  pins pattern B for every model.
- **T-S3, `lasso` -- `sklearn.linear_model.ElasticNet`.** On `Xs` as in
  T-S2, `ElasticNet(alpha = lam, l1_ratio, fit_intercept = add_intercept,
  tol = 1e-12, max_iter = 10_000).fit(Xs, y, sample_weight = w)`. sklearn's
  objective is `(1/(2W)) Σ w (y - Xs b)² + α ρ |b|₁ + ½ α (1-ρ) |b|²`,
  which is our `½ b'Cb - d'b + l1 |b|₁ + ½ l2 |b|²` with `C = corr`, `d =
  cov(xs, y)`, `l1 = lam ρ`, `l2 = lam (1-ρ)`, up to a constant. Exact for
  every path point; unscale as in T-S2.
- **T-S4, `huber` and `quantile` -- `sklearn.linear_model.HuberRegressor`,
  `statsmodels.QuantReg`.** Statistical tier: our IRLS-on-the-prior-
  residual is not their algorithm. Exact tier exists at the limits:
  `huber_delta = 1e9` is T-S1 with `ridge = 0`; `quantile` at `tau =
  0.5` with `halflife = inf` converges to `QuantReg(y, X).fit(q = 0.5)`
  within the IRLS floor `quantile_eps`. For pattern B (C11) the limit
  case is enough: `huber_delta = 1e9, add_intercept = False, standardize
  = True` must equal `LinearRegression(fit_intercept = False)`.
- **T-S5, `kalman`, `standardize = false` -- `filterpy.kalman.KalmanFilter`.**
  `dim_x = k_total, dim_z = 1, F = I, H = z_t (row), Q = diag(q) · d_clock,
  R = obs_var / w, P = p0 · I, x = 0`, `predict()` then `update(y)` per
  row; with `revert_halflife` finite, `F = diag(2^(-d/r_i))` and `Q` added
  after `predict`. `kf.x` is our `beta`, `kf.P` our `P`, `H P Hᵀ + R`
  our `pred_var`, to rounding (Joseph form vs simple form differ in the
  last bits; `rtol 1e-9`). Exact. `pykalman` is the same check with the
  batch filter.
- **T-S6, `ew_class` -- `sklearn.naive_bayes.GaussianNB`,
  `discriminant_analysis`, `scipy.stats.multivariate_normal`.**
  `halflife = inf` makes every in-window weight 1. `diagonal`:
  `GaussianNB(var_smoothing = 0).fit(X, y)` uses population variance and
  matches `precision_prior → 0` to rounding on `predict_proba`. `full`
  and `shared`: sklearn's `QuadraticDiscriminantAnalysis` divides by
  `n - 1`, so compare the density instead -- `multivariate_normal(mean =
  class mean, cov = class cov + r_c I).logpdf(x)` plus `ln π_c`, softmax,
  against our posteriors; `scipy` is the second opinion on the Gaussian,
  sklearn's `_class_means` on the moments. Exact. Under a `window` fit
  them on the in-window rows only.
- **T-S7, `sgd`, `squared` loss -- `sklearn.linear_model.SGDRegressor`.**
  Statistical tier only: sklearn's `partial_fit` standardizes against
  the moments from *before* the row (the order `SgdCfg::scale_features`
  says it replaced), and its `invscaling` schedule counts rows, not
  `n_eff`. Both converge to the same point, so `coef_` and ours agree to
  `atol 1e-2` at 20 000 rows of unit-scale features, and that is enough
  for pattern B (C13): `SGDRegressor(fit_intercept = False, penalty =
  None)` on `X / sqrt(E[x²])`, unscaled, against `add_intercept = false,
  scale_features = true`. The exact check that does not need sklearn is
  `dot(coef, x) == pred`, which every other model meets.
- **T-S8, `ew_cov` under a `window` -- `numpy`.** The windowed moments are
  a weighted mean and covariance of the in-window rows with weights
  `lam^age`, which `numpy.average` and `numpy.cov(aweights = ..,
  ddof = 0)` give directly; `scipy.spatial.distance.mahalanobis(x, mean,
  inv(cov + prior·scale·I))` for `mahal` and `numpy.linalg.eigh` for
  `pc<j>_var`. Exact to `1e-9`. The Rust test `a_window_reports_exactly_
  the_rows_inside_it` already does this for `mean`, `var` and `cov` with
  its own `direct_window` oracle; C14 is the two slots it does not cover.
- **T-S9, every EW accumulator -- `pandas.DataFrame.ewm`.** This is the
  one that should have been first: pandas implements the *same*
  mean-form, adjusted, centred EW recursion. `df.ewm(halflife = h, adjust
  = True, ignore_na = True)` and then `.mean()`, `.var(bias = True)`,
  `.cov(bias = True)`, `.corr()` are `ew_cov`'s `mean`, `var`, `cov`,
  `corr` on a row-count clock **to rounding, at any offset** -- pandas
  keeps `mean_x`, `mean_y` and a centred `cov` and updates them Welford-
  style, so it is a second opinion on the *numerics*, not just the
  algebra. With an irregular clock, `df.ewm(halflife = pd.Timedelta(..),
  times = df["clock"])` is the clock model (`adjust = True` is forced
  there, which is ours). Row weights (`w` on the `step`) are the one
  thing pandas' `ewm` lacks; use `w ≡ 1` or T-S10. The same call on the
  two columns `[x_j, y_t]` is `marginal`'s pair `(j, t)`, and on `y_t`
  alone it is `TargetMoments`. `bias = False` is Kish's correction and
  must **not** be used: it is `n_kish`, not `n_eff`. Exact tier, `rtol
  1e-12` at offset 0 and `1e-9` at offset `1e8` -- the second number is
  the one pattern E fails.
- **T-S10, any windowed or blended accumulator -- `numpy` with explicit
  weights.** Two facts make one oracle serve every site of pattern E.
  (i) A `window` keeps the rows with `age < window` at weight `w ·
  lam^age`: `numpy.cov(X[keep].T, aweights = w_keep, ddof = 0)` and
  `numpy.average(X[keep], weights = w_keep, axis = 0)` are the truncated
  co-moments and means. (ii) A `session_shrink` blend at `f` of a fast
  accumulator (halflife `h`) with its slow twin (halflife `H`) that saw
  the **same rows** is the accumulator with row weight `w · ((1−f) ·
  lam_h^age + f · lam_H^age)` -- write out `af·m_f + as·m_s` with the
  mean-form definitions and the `W'` cancels -- so the same two `numpy`
  calls with that weight vector are the blended moments, exactly, before
  any solve. Both are two-pass (centre, then square), so both are
  correct at any offset. Exact tier, `rtol 1e-9`.
- **T-S11, `marginal`'s `t`, `n_serial`, `t_serial` -- `scipy.stats`,
  `statsmodels`.** At `halflife = inf`, unit weights: `scipy.stats.
  pearsonr(x, y)` is `corr` exactly, and its `statistic` transformed by
  `r·sqrt((n−2)/(1−r²))` is `t` with `n_kish = n` (exact). `statsmodels.
  tsa.stattools.acf(x, nlags = L, adjusted = False)` is `lagcorr_xx` in
  the **statistical** tier only -- it centres every lag at the overall
  mean and divides by the lag-0 variance, where ours centres each leg at
  the pre-row mean; they agree to `atol 0.02` at 5 000 rows. The
  Bartlett factor itself has an exact reference: the `"truncated"` rule
  is the ratio of a long-run variance to a variance under **uniform**
  kernel weights, `statsmodels.stats.sandwich_covariance.cov_hac(...,
  weights_func = weights_uniform, nlags = L)` over `cov_hac(nlags = 0)`
  on the product series `x_t·y_t` (independent series, so the product's
  autocovariance is the product of the two); S18 is what that comparison
  exposes. `weights_bartlett` is the kernel that would fix it.
- **T-S12, `marginal`'s bins and split -- `scipy.stats.binned_statistic`,
  `sklearn.tree`.** With given edges, `halflife = inf`, unit weights:
  `binned_statistic(x, y, statistic = "mean", bins = edges)` is
  `bin_mean_y` and `statistic = "count"` is `bin_n`, exact (`np.var` per
  bin for `bin_var_y`, `ddof = 0`). `DecisionTreeRegressor(max_depth =
  1).fit(x[:, None], y)` chooses its threshold among *all* midpoints and
  ours among the edges, so its impurity decrease over `var(y)` is an
  upper bound on `split_gain`, with equality when `tree_.threshold[0]`
  falls between the same two data points as `split_at`. The existing
  Rust test recomputes the gain by hand over the same edges; the tree is
  the check that the hand recomputation is the right quantity.

## Findings

Prefix: **C** changes a number or a state; **S** a semantic inconsistency
between two places that both look intentional; **P** performance against
a stated invariant; **D** documentation; **V** something a pass could not
confirm and the next one should.

### C1. `solve_standardized` reads the live accumulators under a `window`

`crates/online-core/src/ewridge.rs`, `solve_standardized`, the
`add_intercept` branch. Pattern A.

`solve()` passes the truncated `cov` it got from `view()`, and the
no-intercept branch reads it. The intercept branch reads `self.cov`:

```rust
let means: Vec<f64> = zidx.iter().map(|&z| self.cov.mean(z)).collect();
...
c[i * kf + j] = self.cov.cov(zidx[i + 1], zidx[j + 1]);
```

while `keep` in the same branch reads `cov.raw(...)`. So with `window` +
`standardize` + `add_intercept` the centred Gram and the means are the
**full** history and the right-hand side (`b`, `ybar`) is the windowed
one. The coefficients come from two different histories and the window's
guarantee -- no row older than the window reaches the fit -- fails
silently. The comment above the line ("Materialized up front: the solve
below borrows `self` mutably") is stale.

Fix: `cov.mean(z)`, `cov.cov(..)`. Until fixed, `validate` should refuse
`window` with `standardize`, the way it refuses `window` with
`ridge_decay`.

**Test.** Rust: `a_windowed_fit_is_the_fit_of_the_rows_inside_the_window`
with `standardize = true` -- its `direct_window_fit` oracle is already
standardization-free, so the same assertion applies and fails today by
far more than its `1e-6`. Python second opinion: T-S2 on the in-window
rows with `sample_weight = lam^age`, at a row 3 halflives past a slope
change so the two histories disagree; `window = 2.7 h`, `ridge = 1e-8`,
`atol 1e-6`. Also the negative control: the same test with `standardize
= false` passes today, which localizes the defect to the one branch.

### C2. `view()` falls back to the live state when the window is empty

`crates/online-core/src/ewridge.rs`, `view`, `n_eff`, `sigma2`, `solve`;
**the same code in `lasso.rs`** (`Lasso::view`, `?` on `truncated_mean`).

`view()` uses `?` on `truncated(..)` and on `truncated_mean(..)` for every
target. Both return `None` when the in-window weight is `<= 0`. `view()`
turns that into `None`, and every caller treats `None` as "nothing has aged
out yet, read the live state" -- the same value the early return for
`old.cov.w == 0.0` gives. The two situations are opposites.

When it happens:

- a target that was null on every row inside the window -- a sparse
  long-horizon label beside a dense one -- makes the whole view `None`, so
  **every** target is solved on the unwindowed Gram and `n_eff` reports the
  unwindowed weight (so `min_periods` passes). It flips back when the
  target reappears;
- a clock gap at or beyond the window;
- a tiny negative from rounding in `w_now - f * old.w`.

`window.rs`'s `trim` comment says the model "reports an empty window rather
than stale numbers"; `ewridge` does the reverse. Note also that
`predict` gates a slot on the *untruncated* `self.wj[j] > 0.0`, and that
`step`'s first-solve clause reads `self.cov.n_eff()` (untruncated) where
`predict` reads `self.n_eff()` (truncated).

**`ew_class::view` is the model to copy**: a class whose `truncated`
comes back `None` is replaced by an empty accumulator ("scores nothing
rather than scoring on rows the window excludes") and the view is still
returned. **`ew_cov::view` is the other one** (pass 5): its `match` on
`truncated` has the three-way answer -- borrow when nothing has aged
out, own the truncation, and on `None` own an *emptied* copy
(`set_moments` to zero, "report an empty state rather than dividing by
it") -- which is exactly the shape `ewridge` and `lasso` need.
`marginal::pair` (pass 6) is the per-target version: a `cut` that comes
back `None` reports `n_eff = 0` and NaN for that pair and leaves every
other target alone, which is C2's fix in miniature.

Fix: make `view()` return a three-way answer (live / truncated / empty),
handle the empty case per target (that target's slots NaN, its `r`
untouched) rather than for the model, and clamp the truncated weight at 0
before the test.

**Test.** Rust, two targets, `window = 60`, halflife 25, `y1` null from
row 100 on: (a) `coef[0]` at row 200 equals the same model run with
`n_targets = 1` on `y0` alone, bit for bit -- today it jumps to the
unwindowed fit at the row `y1`'s last in-window row ages out; (b)
`n_eff` is monotone-bounded by the window's weight (`≤ 1/(1-lam)` at
`window ≥ 3h`) rather than jumping to the full history; (c) a clock gap
of `1.5 · window` on one row: `pred` is NaN on the next row (the window
holds one row, below `min_periods`), not a finite number from the live
Gram. Python second opinion: T-S1 on `y0`'s in-window rows at row 200,
`atol 1e-9`. Same three on `lasso` with T-S3.

### C3. A `session_shrink` blend never re-solves

`crates/online-core/src/ewridge.rs`, `blend_toward_long_run`, `step`;
`crates/online-polars/src/stream.rs`, `run_instance`, `predict_chunk`.

`blend_toward_long_run` mixes `cov`, `wj`, `r` and `tm` and leaves `beta`
as it was. `step`'s `due` test has no "blended since the last solve"
clause. With the bank's defaults (`solve_every = halflife / 50`,
`max_rows_between_solves = u32::MAX`) the first rows of a new session are
predicted with the pre-blend coefficients; the blend reaches `pred` only
when the schedule next fires. The point of the feature -- "begin the day
partway back toward the long run" -- is missed on exactly the rows it is
for. The test helper `coefficients_after_blend` documents the staleness
("after a blend the coefficients are stale until the next solve") but
nothing in the stream forces the solve.

`predict_chunk`'s "blended" class is affected the same way: it blends a
*copy* and calls `predict`, which reads the copy's untouched `beta`, so it
writes the same `pred` as the as-is class (only `n_eff` differs). Its
docstring promises the row "is scored by a blended copy".

Fix: `blend_toward_long_run` sets a `stale_beta` flag (or calls `solve()`
when `beta.is_some()`); `due` includes it; `predict_chunk` blends then
solves on the copy.

**Test.** Python, through the bank so the stream's schedule is in play:
the `session_shrink_reverts_toward_the_long_run` stream (slope +1 for
4000 rows, −1 for 300) with a `session` column that changes after row
4300, `session_shrink = 1.0`, default `solve_every`. The first row of
session 2 must have `pred = x · β_slow` where `β_slow` is the solve of
the twin's own accumulators -- read them off `bank.gram()` and solve with
`numpy.linalg.solve(S + ridge D, r)` (second opinion on the solve), or
compare to a second bank with `halflife = long_halflife` and no shrink,
`atol 1e-9`. Today the first row's `pred` equals the *previous* session's
last `pred` slope. Then `bank.predict()` on that row must give the same
number as `fit_predict` did (E31's contract), which today it does not.

### C4. Building a `predict` plan drains the caller's closed-group queue

`python/polars_online/_frame.py`, `_source`.

```python
bank = make_bank()
schema = step(bank, pl.DataFrame(schema=in_schema)).schema
...
closed_schema = bank.closed_groups()
```

`closed_groups()` defaults to `drop=True` (`_polars_online.pyi`). For
`lf.online.predict(bank)` with a `ModelBank`, `make_bank` is `own()`,
which returns the caller's live bank, so building the plan discards every
closed row the caller has fitted and not yet read. `predict` is documented
as leaving the bank untouched.

Fix: `bank.closed_groups(drop=False).clear()` (or a schema-only accessor
on the native side). `DataFrameOnlineNamespace.fit_predict` also calls
`closed_groups()` twice in one expression -- the first drains, the second
supplies the schema -- which is correct only by Python's left-to-right
argument evaluation; take the frame once and use `.clear()` for the schema.

**Test.** Python, in `test_closed_groups.py`: fit the `blocks` frame of
`test_rcov.py` so two groups close; `before = bank.closed_groups(drop =
False).height`; build `lf.online.predict(bank)` and *do not collect it*;
`bank.closed_groups(drop = False).height == before`. Fails today with
`0`. Then collect the plan and check again. No second opinion applies;
this is a side effect, not a number.

### C5. `label_delay` ignores a reset or session boundary on a skipped row

`crates/online-polars/src/stream.rs`, `apply_label_delay`.

The `!plan.accept` branch pushes the plan through and returns without
touching `self.pending`. `run_instance` still applies `reset`, `blend` and
`clear_lags` for that plan, because those run before its `accept` test.
So:

- **reset on a skipped row**: the models restart, `pending` survives, and
  the previous session's rows are replayed into the fresh model;
- **session change or capped gap on a skipped row**: the lag ring is
  cleared at the skipped row, and the rows still waiting from before it
  are released afterwards -- paired across the very break the clear was
  for. This is the L2 failure the comment in the accepted branch
  describes, reached by the other door.

Fix: in the skipped branch, `if plan.reset { self.pending.clear() }` and
`if plan.session_changed || plan.capped { release everything before the
plan }`.

**Test.** Python, in `test_label_delay.py`: two frames that differ only in
whether the first row of session 2 has a null feature. With `session_gap
= "reset"` and `label_delay = 5`, after the null row the second frame's
model must be identical to a *fresh* bank fed session 2 alone (compare
`coef` at the chunk end, `rtol 1e-12`); today it carries five rows of
session 1. With `session_gap = 10.0` and a `marginal` spec with `lags =
[1]` (the one model with a lag ring), the closed row's lag-1 pair count
must equal the pairs *within* each session, computed with `numpy` on the
frame grouped by session (second opinion on the count); today it is
higher by the pairs that straddle the boundary.

### C6. `ridge_decay` + `session_shrink` resets `prior_scale` at every blend

`crates/online-core/src/ewridge.rs`, `blend_toward_long_run`. **Confirmed
in pass 2** (was V1): `EwCov::set_moments(mean, centered, w_sum, q_sum)`
takes no `prior_scale`, and `prior_scale` moves only in `EwCov::update`
(`self.prior_scale *= lam`).

The blend builds `let mut blended = EwCov::new(k)` and fills it with
`set_moments(..)`, so the new accumulator carries a fresh `prior_scale`
and the decaying prior -- "the product of every decay factor the stream
has applied" -- is restored to full strength on every session boundary.
`validate` allows the combination while refusing `window` + `ridge_decay`
for the analogous reason.

Fix: carry `prior_scale` through the blend (the fast side's, or the same
mixture as the weights), or refuse the pair.

**Test.** Rust: `ridge_decay = true, session_shrink = Some(0.0),
long_halflife = Some(h)` versus the same config without a twin, 500 rows,
`blend_toward_long_run()` at row 250, coefficients after row 251 equal to
the bit (`f = 0` is a no-op by `blend_endpoints_are_identity_and_full_
replacement`, so any difference is `prior_scale`). Second opinion: the
same stream through `rls` with the same `ridge` -- `agrees_with_ewridge_
solved_every_row` already holds `rls` and `ridge_decay` to `1e-9` --
which has no twin and so cannot lose the scale; run the agreement across
the blend.

### C7. A zero-weight row after warm-up poisons the lasso's lambda selection

`crates/online-core/src/lasso.rs`, `step`, the selection block. Pattern C.

```rust
let w_new = sel_lam * self.sel_w[j] + weight;
let a = sel_lam * self.sel_w[j] / w_new;
```

with `sel_w[j] == 0` and `weight == 0` is `0/0`. `sel_w` first moves on
the first row whose `pred` is finite; if that row carries weight 0 --
`min_periods = 1`, row 2 with `w = 0`, which hard rule 9 says is legal --
every `sel_err[j][li]` becomes NaN and stays NaN: `err[li] < err[best]`
is then false for every `li`, `sel_idx` sits at 0 (the largest penalty)
and `lam_selected` never moves again. `EwRidge::step` guards the same
recursion with `lam * wj + weight > 0.0`; this block does not.

Fix: the same guard.

**Test.** Rust, in `lambda_selection_tracks_the_out_of_sample_error`'s
frame: `min_periods = 1`, rows `[w=1, w=0, w=1 × 400]`; assert
`sel_err` finite at every row and `lam_selected` at the end equals the
`argmin` of the `numpy`-style sums the test already accumulates (which
skip the zero-weight row). Today `sel_err` is NaN from row 2 and
`lam_selected` is the path's first entry. Add the same row to
`crates/online-core/tests/model_contract.rs`'s zero-weight case for every
model, since hard rule 9 is what it checks.

### C8. Lasso without an intercept solves a centred/uncentred hybrid

`crates/online-core/src/lasso.rs`, `standardized`. Pattern B.

`C` is built from `acc.cov(..)` (centred co-moments) and `s` from centred
variances for every slot, regardless of `add_intercept`. With
`add_intercept = false`, `ybar = 0`, so `d_i = r_i / s_i` is the *raw*
cross-moment, and no intercept is recovered. The KKT system solved is
neither the centred problem (which would need `ybar` and an intercept)
nor the raw one (`E[xx']`, `E[xy]`); its solution is OLS only when the
features have zero mean. `EwRidge::solve_standardized`'s no-intercept
branch uses raw second moments for exactly this reason ("there is no
centering here, so no cancellation either"). Every lasso test uses
`add_intercept = true`.

Fix: without an intercept, scale by `sqrt(raw(i,i))` and drop the mean.

**Test.** Rust: `zero_penalty_matches_ols` with `add_intercept = false`
and `x + 3.0`; the closed form is `E[xx']⁻¹ E[xy]`. Python second opinion:
T-S3 with `fit_intercept = False` on `X / sqrt(E[x²])`, `alpha` over the
path `[0.5, 0.1, 0.0]`, features offset by `+3`, `atol 1e-6` -- and
`every_path_point_satisfies_the_kkt_conditions` rewritten for the raw
objective, which is the check that does not depend on either library.

### C9. `Lasso::predict` reports the unwindowed `n_eff`

`crates/online-core/src/lasso.rs`, `predict`. Pattern D. `n_eff` there is
`self.cov.n_eff()`, the full-history weight; `Lasso::n_eff()` (windowed)
exists and is what `EwRidge::predict` uses. Under a `window` a lasso's
`n_eff` field and its `min_periods` gate are on the whole history while
its fit and `lam_selected` are on the window, and the two models disagree
on what `n_eff` means under the same option. Use `self.n_eff()` (after
C2 is fixed, or the fallback makes it worse).

**Test.** Python, in `test_window.py`: `ewridge` and `lasso` with
`lasso_path = [0.0]`, the same `window` and halflife on the same frame,
`n_eff` fields equal at every row (`rtol 1e-12`). Today they diverge at
the first row where anything ages out. Second opinion on the number
itself: `Σ lam^age` over the in-window rows in `numpy`.

### C10. Kalman standardizes without an intercept by centring anyway

`crates/online-core/src/kalman.rs`, `scales_into`, `standardized`,
`coefficients`. Pattern B. With `standardize = true` (the default) and
`add_intercept = false`, `off = 0` and every slot is `(x_i - m_i) / s_i`.
The filter's prediction is `Σ b_i (x_i - m_i)/s_i`, which carries a hidden
intercept `-Σ b_i m_i / s_i`; `coefficients()` returns `b_i / s_i` and has
no slot for it, so `coef · x != pred`. `EwRidge` avoids centring in its
no-intercept path. The `river` cross-check test turns `standardize` off,
so nothing pins this.

Fix: without an intercept, scale only (`x_i / s_i` with `s` from the raw
second moment), or document that `coef` does not reproduce `pred` there.

**Test.** Python: `add_intercept = False, standardize = True`, features
`x + 5`; at every row `dot(coef_row, x_row) == pred_row` to `rtol 1e-9`
(the contract every other model meets; `test_coef.py` is where it
belongs). Today the difference is `-Σ b_i m_i / s_i`, order 5. Second
opinion, after the fix: T-S5 on `X / sqrt(E[x²])` with `q = 0`,
`obs_var` fixed and `p0` large is `Ridge(fit_intercept = False, alpha =
obs_var / p0)` on the scaled features (the Bayesian-regression identity
the existing `unstandardized_with_no_process_noise_is_bayesian_
regression` test uses), unscaled by `sqrt(E[x²])`.

### C11. Robust standardizes without an intercept by centring anyway

`crates/online-core/src/robust.rs`, `solve_standardized`. Pattern B;
**confirmed in pass 3** (was V6). With `off = 0`: `c` is the centred
co-moment matrix, `s` the centred standard deviations, `ybar = 0` so
`bsub[i] = b[i] / s[i]` is the raw cross-moment, and no intercept is
recovered -- C8's hybrid system exactly, with the same fix (the raw
second-moment scaling `EwRidge` uses). Every robust test uses
`add_intercept = true`, and the comment "Same scheme as
EwRidge::solve_standardized" is true only of the intercept branch.

**Test.** Rust: `huber_matches_least_squares_without_outliers` with
`add_intercept = false, standardize = true` and `x + 3`; the closed form
is the raw OLS. Python second opinion: T-S4's limit case,
`HuberRegressor` is not needed -- `LinearRegression(fit_intercept =
False)` on the offset features, `atol 1e-6`. For the quantile loss the
same at `tau = 0.5` against `statsmodels.QuantReg(y, X).fit(q = 0.5)`
with no constant column, statistical tier (`atol 0.05` at 20 000 rows).

### C12. `ew_class`'s `full` shape factorizes the live covariance under a `window`

`crates/online-core/src/ewclass.rs`, `score`, the `Covariance::Full` arm.
Pattern A.

`score` builds `view()` and reads the class means and weights from it,
but the factor comes from `self.class_matrix(c)`, which reads
`self.classes[c].comoments()` -- the untruncated moments. So under a
`window` the `full` shape's quadratic form and log-determinant are those
of the whole history, applied to a `delta` measured from the windowed
mean. `shared` reads `classes[c].comoments()` (the view) and `diagonal`
reads `cov.var(i)` (the view), so only `full` is wrong. The window's own
doc says its cost is "one `O(k^3)` factorization per class per row"
because "a window's truncated covariance moves every row" -- that
factorization is paid every row (`invalidate_all`) to factorize the wrong
matrix.

Fix: `class_matrix` takes the accumulator to read (`&classes[c]`), and the
cache is keyed off it.

**Test.** Rust: `shared_equals_full_when_the_classes_share_their_moments`
with `window = Some(150.0)` -- the twin construction makes `full` and
`shared` the same model, and `shared` reads the view, so they must still
agree to `1e-9`; today `full` disagrees once anything ages out. Then
`every_shape_matches_the_from_scratch_oracle` with a window, where the
oracle sums over `rows[i - in_window..i]` only -- a five-line change to
the best test in the crate. Python second opinion: T-S6 on the in-window
rows, `halflife = inf`, `precision_prior = 1e-9`: `scipy.stats.
multivariate_normal.logpdf` with the windowed class moments from `numpy`
(`ddof = 0`) reproduces `p_<c>` to `atol 1e-9`; and `GaussianNB` for the
`diagonal` shape, which passes today and is the negative control.

### C13. `sgd` standardizes without an intercept by centring anyway

`crates/online-core/src/sgd.rs`, `standardized`, `scales`,
`coefficients`. Pattern B; **closes V8, the other way.**

`standardized` computes `(xi - mean) / scale` for every feature whatever
`off` is, and `scales` divides by the centred sd. With `scale_features =
true, add_intercept = false` the step is taken on centred coordinates,
the prediction is `Σ b_i (x_i - m_i)/s_i` with the hidden intercept `-Σ
b_i m_i / s_i`, and `coefficients()` (`b_i / scales[i]`, no intercept
slot to put the shift in) reports slopes that do not reproduce `pred`:
C10's situation exactly. `constraint.rs` describes `scale_features` as
scale-only (`b_i = c_i · scale_i`, no mean shift) -- that is a
description of the fix, not of the code, and under a constraint the
projection is taken in a coordinate system with a shift the bounds do
not know about. Every `sgd` test that turns `scale_features` on keeps
the intercept.

Fix: in `standardized` and `scales`, when `off == 0` divide by `sqrt(v +
mean²)` -- the raw second moment, which both already form for the
usability test -- and do not subtract the mean. `coefficients` needs no
change; the projection follows `scales`.

**Test.** Python: C10's `dot(coef_row, x_row) == pred_row` at
`add_intercept = False, scale_features = True`, features `x + 5`, `rtol
1e-9` -- today the difference is `-Σ b_i m_i / s_i`, order 5, on every
row. Rust: `scaling_reports_coefficients_in_the_callers_units` with
`add_intercept = false` on the same `500 ± 100` stream and `y = 0.05 x`:
the slope converges to `0.05` either way (with a target through the
origin the centred and raw slopes coincide), but `step.pred` on the last
row is `0.05 · (x - 500)`, off by 25. Second opinion: T-S7, statistical
tier.

### C14. `ew_cov`'s `mahal` and PCA refresh read the live accumulator under a `window`

`crates/online-core/src/ewcov.rs`, `read` (`EwCovStat::Mahal =>
self.mahal(x)`), `mahal`, `refresh_pca`, `step`. Pattern A.

`predict` builds the view and passes it to `read`, which uses it for
`mean`, `var`, `std`, `cov`, `corr`, `pcorr` and the PC *score*. Two
slots go elsewhere: `mahal(&self, x)` reads `self.cov.means()` and
`self.cov.comoments()`, and `refresh_pca` reads `self.cov.comoments()`
and gates on `self.cov.n_eff()`. Under a `window`:

- `mahal` is the distance from the whole history's mean in the whole
  history's metric, emitted beside a windowed mean and variance;
- `pc<j>_var`, `pc<j>_share` and the loadings are the whole history's
  eigenpairs, while `pc<j>_score` is `v · (x - μ_window)` -- a loading
  from one history applied about the centre of another.

`validate` refuses `window` with `lags` ("would report a windowed corr
beside an unwindowed lagcorr") and with `mahal_quantiles` ("compares
two different histories") -- the same argument, applied to two of the
four cases and not the other two.

Fix: `mahal(&self, cov: &EwCov, x: &[f64])` taking the view, and
`refresh_pca` from `self.view()` (it runs after the update, so the view
is rebuilt there -- once per `pca_every` rows, O(k²) beside the O(k³)
eigensolve). Until then, refuse `window` with `mahal` and with `pca`
alongside the other two.

**Test.** Rust: `nothing_older_than_the_window_can_move_the_answer` with
`stats = [Mean, Mahal]`, `precision_prior = 1e-6`, `pca = 1`, `pca_every
= 1`: the poisoned prefix (`1e6` on rows outside the window) must leave
`mahal` and `pc0_var` where the clean run has them, as it already leaves
the mean (the negative control in the same test). Today both move by
orders of magnitude. Second opinion: T-S8 -- `scipy.spatial.distance.
mahalanobis` and `numpy.linalg.eigh` on the in-window rows.

### C15. `ew_cov`'s `mahal_quantiles` slot arithmetic does not know `LagCorr`

`crates/online-core/src/ewcov.rs`, `step`:

```rust
.take_while(|s| **s != EwCovStat::Mahal)
.map(|s| match s {
    EwCovStat::Mean | EwCovStat::Var | EwCovStat::Std => k,
    _ => k * (k - 1) / 2,
})
```

`LagCorr` is `lags.len() · k²` slots, not `k(k−1)/2`. With `stats =
[.., LagCorr, .., Mahal]` (legal: the refusal is `window` + `lags`, not
`lags` + `mahal`), the index lands short, and the P² estimators are fed
a lagged correlation instead of the distance: `mahal_q<p>` is a quantile
of the wrong series, in range (pairs < lags·k²) so no panic and no NaN.
`n_outputs`, `labels` and `read` walk the stats correctly; this is a
fourth walk with arithmetic of its own, and
`n_outputs_and_labels_agree_slot_for_slot` pins only the first three.

Fix: one `fn width(&self, stat: EwCovStat) -> usize` used by all four.

**Test.** `mahal_quantiles_track_the_scores_seen_so_far` with `stats =
[LagCorr, Mahal]`, `lags = [1]`, `k = 2`, `mahal_quantiles = [0.5]`: the
oracle fed `out[4]` (the true `mahal` slot) disagrees with `mahal_q`
today, which is fed `out[1]` (`lagcorr_a_b_l1`). No second opinion
applies; it is an index.

### C16. The two `session_shrink` blends re-centre through the raw second moment

`crates/online-core/src/ewcov.rs`, `TargetMoments::blend`, and
`crates/online-core/src/ewridge.rs`, `blend_toward_long_run` -- **V9
confirmed in pass 6**; the `k×k` mixture has the same shape as the
per-target one:

```rust
// ewcov.rs
let raw = a * (self.var[t] + self.mean[t] * self.mean[t])
    + b * (other.var[t] + other.mean[t] * other.mean[t]);
self.var[t] = raw - mean * mean;
// ewridge.rs
let raw = af * self.cov.raw(i, j) + as_ * slow.cov.raw(i, j);
mixed_c[i * k + j] = raw - mixed_mean[i] * mixed_mean[j];
```

Pattern E. The `ewcov.rs` module doc opens by explaining why the
accumulator stopped computing `E[x²] − m²`: at a large offset the
subtraction destroys the variance. A target at `1e8` with unit variance
has `var + m² = 1e16 + 1`, which is `1e16` in f64 (the ulp there is 2),
and the blended variance comes back as `0` -- or 2, or 4, depending on
how the two terms round. Both run on every `session_shrink` blend, so
from the first session boundary the Gram's centred co-moments (and with
them every `standardize` solve, C1 aside) and `sigma`, `resid_z`, `r2`
for a large-level target are wrong; targets and features near zero
(returns) are unaffected, which is why nothing has shown it. The test
`blend_is_the_weight_respecting_mixture` pins the raw-then-recentre form
as its *oracle* ("raw moments mix linearly; the centred moment must then
be re-derived against the mixed mean"), at offset `0.5`, so it passes
and must be rewritten with the fix.

The exact centred mixture has no cancellation: `var = a·var + b·var' +
a·b·(m − m')²`, and for the matrix `C = a·C_f + b·C_s + a·b·ΔΔᵀ` with `Δ
= m_f − m_s`. It is the same `a`, `b` and means the code already has.

**Test.** Library first: T-S10 (ii) -- the pre-blend twins saw the same
rows, so `numpy.cov(X.T, aweights = (1−f)·lam_h^age + f·lam_H^age, ddof =
0)` on rows at `x + 1e8` *is* the blended Gram, and `numpy.average(y,
weights = same)` / the weighted variance of `y + 1e8` is `TargetMoments`
after the blend; `rtol 1e-9` fails today at offset `1e8` with the
variance at `0` and passes at offset `0`. Then Rust:
`blending_target_moments_with_a_copy_is_the_identity` learned from `y =
1e8 + cos(..)`, `1e-12` on the variance; and `blend_is_the_weight_
respecting_mixture` with `blended_pair`'s features at `x + 1e8` and its
oracle rewritten to the centred identity. The bank-level check that
does not need any library: `sigma` on the first row of session 2 with
`session_shrink = 0.5` on a price-level target equals `sigma` on the
last row of session 1 to within the blend's own mixing (today: `0`,
then a jump).

### C17. `window::truncated` and `marginal::cut` re-centre through the raw second moment

`crates/online-core/src/window.rs`, `truncated`:

```rust
// Back to raw second moments, subtract, and re-centre on the
// window's own mean.
let now = w_now * (cov.comoments()[i * k + j] + cov.mean(i) * cov.mean(j));
let then = old.w * (old.c[i * k + j] + old.m[i] * old.m[j]);
cen[i * k + j] = (now - f * then) / w - mean[i] * mean[j];
```

and `crates/online-core/src/marginal.rs`, `cut`, the same three lines
one pair at a time (`raw / wn - a * b`). Pattern E, and the one that
matters most: `truncated` is the shared truncation **every** windowed
model calls -- `ewridge` (through `view`, and so every windowed solve,
`sigma2` aside), `lasso`, `ew_class`, `ew_cov` -- and `cut` is
`marginal`'s own copy. Its doc says the subtraction "is a difference of
positives, so it loses precision in proportion to the fraction
discarded"; that is true and is not the problem. The problem is the
round trip through `E[x²]`, which at offset `1e8` and unit variance
leaves the windowed variance with a resolution of about `2` (the terms
are `~2e17`, ulp `32`, divided by `w ≈ 18`), i.e. nothing; at `1e7`
about `1%`; at `1e6` about `1e-4`. Every existing window test runs at
offset `0` (`ewridge`, `marginal`) or `100` (`ew_cov`, `x ∈ [100, 110]`,
tolerance `1e-8` relative, which passes at that level and not far
above). So a windowed `ew_cov` on a price, a windowed `marginal` pair
against a price-level target, a windowed `standardize` fit on a level
feature, and a windowed `ew_class` on level features are all wrong today,
silently, by an amount that grows as the square of the level.

Fix: the centred truncation. With `W` the live weight, `W_u' = f·W_u` the
decayed snapshot weight and `W_R = W − W_u'`:

```text
m_R   = (W·m − W_u'·m_u) / W_R                       (as now: a mean, fine)
W_R·C_R = W·C − W_u'·C_u − (W_u'·W / W_R)·(m_u − m)(m_u − m)ᵀ
```

(the pooling identity of pattern E solved for the remainder; `(m_u − m)`
is a difference of two means, order `σ`, at any offset). `cut` is the
same with `(m_u,x − m_x)(m_u,y − m_y)` for the cross term. This also
closes **V3**: `cen[i,i]` today is not floored, and with the centred
form a slightly negative diagonal can only come from the "difference of
positives" loss the doc already accepts, so `.max(0.0)` on the diagonal
is then the right floor.

**Test.** Library first, T-S10 (i): `numpy.cov(X[keep].T, aweights =
lam^age, ddof = 0)` on rows at `x + 1e8` against `ew_cov`'s windowed
`var` and `cov`, `rtol 1e-9` -- fails today with `var` at `0` or `±2`;
and T-S9's `pandas.ewm(...).var(bias = True)` at the same offset with
`window = None` as the negative control, which passes today (the live
accumulator is Welford). Then the four Rust oracles already in the
crate, each at offset `1e8` instead of `0`: `a_window_reports_exactly_
the_rows_inside_it` (`ew_cov`), `a_windowed_pair_is_the_pair_of_the_
rows_inside_the_window` (`marginal`), `a_windowed_fit_is_the_fit_of_the_
rows_inside_the_window` with `standardize = true` (`ewridge` -- after
C1, or the two defects mask each other), and `every_shape_matches_the_
from_scratch_oracle` under a window (`ew_class`, C12's test). All four
oracles are two-pass sums and correct at any offset; all four fail today
at `1e8` and pass at `0`, which localizes the defect to the truncation.

### C18. `marginal` allows `window` with `lags`, and the lag ring is never truncated

`crates/online-core/src/marginal.rs`, `MarginalCfg::validate`, `pair`.
Pattern A.

`EwCovCfg::validate` refuses `window` + `lags`: "the lagged co-moments
are a second accumulator with its own ring, and truncating one and not
the other would report a windowed corr beside an unwindowed lagcorr".
`MarginalCfg::validate` refuses `window` + `bins` on a memory argument
and says nothing about `lags`. `pair` then divides the **live** lagged
co-moments (`lag.cxx(li, t, j)`, whole history, no snapshot) by the
**windowed** standard deviations (`sd_x`, `sd_y` from `cut`), so under a
window every `lagcorr_*` is a hybrid of two histories -- and `n_serial`,
`t_serial`, `phi_x`, `phi_y` and `split_gain_t`'s count are built from
it. A lag ring *can* be snapshotted (`MarginalLags` is normalized moments
with the same decay riding on `W_t`, so `A(t) − f·A(u)` applies to it as
to everything else) -- that is the question for `ewlagcov.rs` and
`marglag.rs` on the next pass. Until then, the two validators should
say the same thing.

**Test.** Library first, T-S11: at `halflife = inf`, unit weights,
`window` covering the last 500 of 5 000 rows of an AR(1) `x`, and a
regime change in `phi` at row 4 500: `statsmodels.tsa.stattools.acf(x[
-500:], nlags = 3)` is the windowed autocorrelation and `acf(x, nlags =
3)` the live one; `lagcorr_xx` today sits between the two (live
covariance over windowed variance) and after the fix matches the first
to `atol 0.02`. Rust: `lag_moments_hold_where_the_target_is_missing`'s
stream with `window = Some(200.0)` and a `phi` change, `lagcorr_xx[0]`
against a longhand `Σ_in_window lam^age·(x_t − m)(x_{t−1} − m) / var`,
which today it does not match; and `validation_rejects_each_bad_field`
with `window` + `lags` expecting the same refusal `ew_cov` gives.

### S1. `sigma` and `resid_z` are not windowed at the bank level

`ewridge.rs` `sigma2` doc: "Under a `window` this is the variance *inside*
it, so `sigma` and `resid_z` describe the same rows the fit does"; and
`RidgeMoments` carries `wsig` / `sig2` for that. But the bank's `sigma`
field is `Stream::resid_var` (`stream.rs`, `run_instance`), an unwindowed
EW mean per slot on the instance's decay; `EwRidge::sigma2` is never read
by the stream. So a windowed spec reports a windowed fit beside an
unwindowed spread -- the outcome the model's own comment says it exists to
prevent. Either route the model's `sigma2` through for windowed specs or
correct the docstring.

**Test.** Python, `emit_sigma = True`, a `window` shorter than the
residual history, a burst of large residuals that then ages out of the
window: `sigma` must fall back to the pre-burst level once the burst is
older than the window (the model-level `sigma2()` does; the bank's field
today does not). Second opinion: `numpy` -- `sqrt(Σ lam^age r²/Σ lam^age)`
over the in-window residuals the bank itself emitted.

### S2. Per-target `min_periods` gates on the shared `n_eff`

`stream.rs`, `step_n_eff_below`. Each target's threshold is compared with
the model's single `n_eff` -- the feature-side weight, which hard rule 8
defines and which is the same for every target. A target that is null on
most rows passes warm-up on the strength of the feature count, not its
own evidence (`EwRidge::target_weights` has the per-target number). This
is consistent with rule 8 and not with what "per-target warm-up" (E7)
suggests. Decide which is meant and document it; if the latter, the trait
needs a per-target weight.

**Test.** Whichever rule is chosen, pin it: two targets, `y1` null on
nine rows in ten, `min_periods = [5, 5]`; the first finite `pred_y1` is
at row 5 under the shared rule and row 50 under the per-target one.
Today it is row 5, which the E7 doc does not lead a reader to expect.

### S3. Skipped-row time is folded without the cap

`clock.rs`, `advance`. Each row's delta is clamped at `max_dclock`, and a
skipped row's clamped delta is added to `pending`; the next accepted row
receives `pending + d`, which can be `n × max_dclock` after `n` skipped
rows. `max_dclock` is documented as a ceiling on the delta a model sees
("a weekend does not decay the state to nothing") and that holds per row,
not for the folded total. Probably the cap belongs on the total as well.

**Test.** Rust, in `clock.rs`: `max_dclock = 60`, one accepted row, then
ten skipped rows 100 apart, then an accepted row; `d_clock` is `600`
today and the test should say which of `60` or `600` is meant. Python:
`n_eff` after the same rows against `numpy`'s `lam^d` with the chosen
rule.

### S4. Two `ModelState` variants share the kind string `"ew_cov"`

`model.rs`, `kind()`: `EwCov` and `EwCovModel` both answer `"ew_cov"`. A
`WrongModel { found: "ew_cov" }` cannot say which, and
`AnyModel::restore`'s error for the bare accumulator reads "expected a
bank-supported model, found ew_cov" -- but `ew_cov` is a bank-supported
model. Give the bare accumulator its own kind.

**Test.** `restoring_the_wrong_model_names_both` (ewclass.rs) with the
bare `EwCov` state: `found` must differ from `EwCovModel`'s kind.

### S5. A single-combo `ew_ridge` loses its combo metadata

`stream.rs`, `combos`: with one ridge and one feature set it returns
`Combo::default()` (`ridge: None`), while a grid carries `ridge:
Some(r)` and `feature_set`. The struct exists "so metadata can never drift
from the string"; a reader of the metadata for the single-combo case gets
nothing. Fill the values whatever the count; only `label` should be empty.

**Test.** `test_output_index.py`: `output_index(spec)["ridge"]` for a
single-ridge spec is the ridge, not null.

### S6. `window.rs` disagrees with itself on the boundary row

Module doc: a snapshot taken before the row at `t_j` carries everything
strictly older than `t_j`, so subtracting it retains `t_j`. `truncated`'s
doc: "everything at or before the snapshot's clock removed". `trim`'s
comment on a single stale entry: "it will subtract the whole accumulator"
-- it subtracts the state *before* the last learned row, so that row
stays in. The module doc is the correct one. Also: `trim` has an `if`
whose body is only a comment, and `new` applies `every.max(1)` after
already refusing `every == 0`.

**Test.** Rust, `window.rs`: after a gap of `2 · window`, `truncated`
against the single remaining snapshot leaves exactly one row's weight
(`w == f · w_u`), not zero -- pins which of the two comments is true.

### S7. `EwRidgeCfg::validate` gaps

Pass 2 (V2): `Spec::validate` refuses a repeated value in a grid
(`first_duplicate`, "ridge lists {} more than once") and a feature set
that names an unknown feature. Not found at either layer: feature-set
names unique (duplicates give duplicate output labels -- `deco` refuses
the same for its blocks in `stream.rs`); a column repeated within a set
(a repeated index duplicates a row and column of the sub-Gram, which is
then rescued by the jitter ladder and counted as a failure instead of
refused); `ridge` values finite and `>= 0`; `min_periods >= 0`; the decay
(`halflife > 0`, `lam` in `(0, 1]`). Whichever layer owns them, the
model's own `new` should not accept a config that cannot be solved.

**Test.** `test_spec_validation.py`: `feature_sets = [("a", ["x0"]),
("a", ["x1"])]` and `[("a", ["x0", "x0"])]` each raise `ValueError`
naming the set; `ridge = [-1.0]`, `[float("nan")]`, `halflife = 0.0`
likewise. Today the first two build and the duplicated column shows as
`solve_failures > 0`.

### S8. `AnyModel::solve_failures` doc vs list

Doc: "Models that do not factorize (rls, kalman, ftrl) report 0". The
`0` arm also covers `ew_cov` (`mahal` and `partial_corr` solve),
`kalman` (inverts the innovation covariance) and others; and see S11 for
`lasso`, which is in the counting arm and never counts. Either count
their failures or say "models that do not count them".

**Test.** Documentation; `test_outputs_doc.py` is where the field's
meaning is pinned per model.

### S9. Kalman: a zero-weight row with a present target skips the per-target decay

`kalman.rs`, `step`. Pattern C. The `None` arm decays `wj` and `wsig`;
the `weight <= 0.0 { continue }` arm, reached with a present target,
decays neither (the standardizer's `n_eff` does decay, through
`stats.update(.., lam, 0)`). `EwRidge` decays both on that row.
Chunk-invariant, and `wj` only gates `> 0`, but `wsig`'s memory grows by
every zero-weight row, so `sigma2` -- and through it `q_i` and the
innovation variance -- follows a recursion the docstring does not
describe. Move the decay above the `weight` test.

**Test.** Rust: `a_null_target_decays_its_weights_and_leaves_the_filter_
alone` duplicated with `Some(-500.0)` at weight 0 in place of `None`;
`wsig` must be `wsig · lam` in both. Python second opinion: T-S5 with
`R = obs_var / w` and `w = 0` handled as "no update" -- `filterpy`'s
`predict()` without `update()` -- and our `sigma2` against the EW
recursion in `numpy` that skips the row but decays.

### S10. RLS never predicts from `coef_prior` before its first learned row

`rls.rs`, `predict`, the `self.seen` gate. `EwRidge` with `ridge_decay` +
`coef_prior` -- the mode `agrees_with_ewridge_solved_every_row` says is
the same model -- predicts from the prior as soon as `min_periods`
allows; RLS returns NaN until a row has been learned, whatever the prior.
The warm start ("begin at yesterday's fit") is the stated purpose of
`coef_prior`. Drop the gate when a prior is set, or document the
difference beside the agreement test. Minor beside it: `#[serde(try_from
= "RlsV2")]` cannot fail, and the name reads as if a migration exists;
lib.rs's schema-2 note ("schema-1 rls states are converted on load") is
history now that `MIN_SCHEMA_VERSION` is 6 -- mark it so.

**Test.** Rust: `agrees_with_ewridge_solved_every_row` with `coef_prior =
Some(..)` and `min_periods = 0` on both, compared from row 0 rather than
row 6. Today row 0 is NaN on one side and `x · prior` on the other.

### S11. `Lasso::solve_failures` is never written

`lasso.rs`: the field is `pub`, `AnyModel::solve_failures` reports it,
and nothing increments it. Coordinate descent that leaves `max_cd_iters`
without meeting `cd_tol` is the failure this model has; count it.

**Test.** Rust: `max_cd_iters = 1, cd_tol = 0` on
`every_path_point_satisfies_the_kkt_conditions`' frame; `solve_failures
> 0` and the KKT residual is large, which is what the count is for.

### S12. `atomic.rs`: Windows and durability caveats missing

`crates/online-polars/src/atomic.rs`. The module doc says the rename
replaces "on both target platforms". On Windows `MoveFileExW` over a
destination another process holds open **without** `FILE_SHARE_DELETE`
fails with a sharing violation: a Python `open(path, "rb")`, a viewer, an
indexer. Rust's own `File` opens with share-delete, so the two-writer
test passes and says nothing about this. Say it, and consider a retry.
Two smaller ones: the parent directory is not fsynced after the rename,
so the rename itself is not durable on POSIX (inside the "old or new"
contract, but worth a sentence); a kill between `create` and `commit`
leaves `.name.tmpPID-seq` behind and nothing sweeps it on the next run.

**Test.** Python, Windows-only in `test_portability.py`: hold
`open(path, "rb")` on a saved state and call `bank.save(path)`; today it
raises `PermissionError`, and the test pins that (rather than a hang or
a torn file) until a retry is added, then pins the retry.

### S13. Robust: two asymmetries in `step` and `solve`

`crates/online-core/src/robust.rs`. Pattern C for the first.

- In `step`, the `None` arm decays `cov`, `wj` **and** `wsig`; the
  `w.is_nan() || w <= 0.0` arm (a zero weight, or a NaN robust weight)
  decays `cov` and `wj` and **not** `wsig`. S9's effect, in the model
  whose weights depend most on `sigma2`.
- In `solve`, the unstandardized path counts a total failure
  (`None => self.solve_failures += 1`); the standardized path returns
  through `solve_spd(..)?` before its increment, so a singular
  standardized solve keeps the previous fit *and* reports nothing.

**Test.** Rust: `a_solve_failure_is_counted_and_the_previous_fit_is_kept`
with `standardize = true` (fails today: `solve_failures == 0`); and the
zero-weight variant of `null_targets_decay_the_residual_variance_weight_
without_adding_to_it` ported here.

### S14. `ew_class` reports the whole-history `n_eff` under a `window`

`ewclass.rs`: `n_eff()` and the `min_periods` gate in `score` read
`self.n_eff`, the live weight, while every class weight, mean and (after
C12) covariance the row is scored on is windowed. Pattern D, the
`ew_class` instance; the snapshot already carries `n_eff * lam`, so the
windowed number is one subtraction away. Decide with C9.

**Test.** As C9's, with `ew_class` beside `ewridge` on the same window.

### S15. `EwCovModel::n_eff()` is the live weight under a `window`

`ewcov.rs`: `predict` emits the view's `n_eff` and gates `min_periods`
on it (its comment states the windowed rule in so many words); the
accessor `EwCovModel::n_eff()` returns `self.cov.n_eff()`, and the PCA
refresh gate in `step` reads the same live number (C14). Pattern D, the
fourth instance, and the one model whose `predict` doc already chose.
`the_model_withholds_statistics_until_min_periods` pins `m.n_eff() ==
step.n_eff + 1` without a window; with one the two diverge at the first
row anything ages out. Check where the stream reads the accessor;
decide with C9 and S14 -- three models now say "whole history" in one
place and "window" in another.

**Test.** As C9's, with `ew_cov` beside `ewridge`, and the accessor
against the emitted field in the Rust test above with `window =
Some(30.0)`.

### S16. An `sgd` state with `scale_features = true` and no `scaler` loads and runs unscaled

`sgd.rs`, `TryFrom<SgdV3>`. The shape check is `scaler.is_some_and(|sc|
sc.k() != k)`, so `scaler: None` passes whatever `cfg.scale_features`
says, and `scaler` is `#[serde(default)]`. The `SgdV3` doc names the
danger ("a renamed optional field would default to `None` on an old
file -- silently dropping the standardization") and keeps the field
name to avoid it, but a file that lacks the key for any other reason --
a hand-edited JSON export, a truncated write -- is exactly that case,
and loads as a model that standardizes nothing on the way in and
unscales nothing on the way out, predicting in raw coordinates with
coefficients learned in standardized ones. `g2` has the same gap in the
other direction: its rows are length-checked, its count is not, so
`schedule = AdaGrad` with `g2 = []` indexes out of bounds in `step` -- a
panic, which is the better failure, but not a diagnosis.

Fix: `cfg.scale_features != scaler.is_some()` and `matches!(cfg.schedule,
AdaGrad) != (g2.len() == m)` both refuse with the existing "wrong shape"
error (a better message names the field).

**Test.** `a_state_without_a_scaler_loads_in_every_layout` with
`scale_features = true`: removing the `scaler` key must give `Err`, not
a model with `scaler.is_none()`. Same for `schedule = AdaGrad` with
`g2` emptied.

### S17. `Marginal::predict` emits the live `n_eff`; the accessor returns the windowed one

`marginal.rs`: `n_eff()` and `target_weight()` subtract the boundary and
are what `pair`'s `min_periods` gate reads; `predict` returns `n_eff:
self.w_sum`, the whole history, and that is the `Step` the stream sees.
Pattern D's fifth instance, and the mirror image of `ew_cov` (S15): there
the emitted field is windowed and the accessor live, here the reverse.
So a windowed `marginal` in a bank reports a whole-history `n_eff` column
beside pairs gated on the in-window weight, and the stream's own
`min_periods` gate (`step_n_eff_below`, S2) is on a different number
from the model's. One line either way; the rule is what needs deciding
(C9, S14, S15).

**Test.** As C9's. The bank-level number: `n_eff` from a windowed
`marginal` spec must stop growing once the window fills, as `ew_ridge`'s
does on the same frame; today it rises for the life of the stream.
Second opinion on the value it should settle at: `Σ lam^age` over the
in-window rows in `numpy`, the same sum T-S10 (i) forms.

### S18. `marginal`'s `"truncated"` serial rule floors the factor at `f64::MIN_POSITIVE` and uses no kernel

`marginal.rs`, `serial_factor`, `SerialRule::Truncated`: `(1.0 + 2.0 *
sum).max(f64::MIN_POSITIVE)`. A negatively autocorrelated pair -- two
mean-reverting series, `rho_x(1) = rho_y(1) = −0.8` -- gives `1 + 2·0.64
= 2.28` (fine), but `rho_x(1) = 0.8, rho_y(1) = −0.8` gives `1 − 1.28 <
0`, the floor fires, and `n_serial = n_kish / 5e-324 = +inf`, `t_serial
= ±inf` (or NaN at `corr = 0`, from `0 · inf`). A factor at or below zero
is an estimate outside the parameter space, and `Geometric` already
answers that case with NaN ("say nothing rather than a negative count");
`Truncated` should do the same rather than report infinite evidence.
The cause is the missing kernel: a truncated sum of `rho_x·rho_y` with
unit weights is not guaranteed positive, which is the whole reason
Newey-West uses Bartlett weights `1 − l/(L+1)` (positive semi-definite
by construction). With those weights the rule is the standard HAC
correction, and a third rule, or a `weights` option on this one, would
give the user the estimator every econometrics library ships.

**Test.** Library first, T-S11: `statsmodels.stats.sandwich_covariance.
cov_hac` on the product series `x_t·y_t` with `weights_func =
weights_uniform, nlags = L` over `nlags = 0` reproduces our factor to
`rtol 1e-9` at `halflife = inf` (exact: both are the same unweighted
sum over the same sample autocovariances, up to statsmodels' `n`
divisors, which cancel in the ratio) -- and goes to zero or negative on
the mixed-sign pair where ours goes to `MIN_POSITIVE`; with
`weights_bartlett` it stays positive, which is the fix's reference.
Rust: `serial_factor(Truncated, &[1], &[0.8], &[-0.8])` must return NaN,
not `(5e-324, ..)`; `t_serial` on that pair NaN, not `±inf`.

### P1. A windowed `ewridge` allocates an O(k²) view on every row

`ewridge.rs`: `predict` calls `n_eff()`, which calls `view()`, which
clones the `k × k` accumulator through `truncated` and builds per-target
vectors -- on every row, and again in `solve()`. `model.rs` states "no
allocation in the hot path after warmup" as an invariant of `step`. Keep
the truncated weight as one scalar for `n_eff`/`predict` and build the
full view only at solve time. **`lasso.rs` is worse**: `view()` is called
inside the per-target selection loop of `step`, `m` times a row.
`ew_class` computes it once per `score`, which is the right cadence for
its use, but also once more in `class_weights()`.

**Test.** `crates/online-core/examples/*_bench.rs` pattern: `k = 200`,
windowed vs unwindowed `ewridge` with `solve_every = 50`; the windowed
step must not be `O(k²)` slower between solves. Numbers, not a pass/fail.

### P2. `Instance::reset` builds every halflife instance to keep one

`stream.rs`: `build_models(spec).swap_remove(self.mi)`. A five-halflife
grid under `drift_action = "reset"` builds five models per reset per
instance. Add a `build_one_at(spec, mi)`.

### P3. An accumulate-only `ew_cov` under a `window` builds the O(k²) view to read one scalar

`ewcov.rs`, `predict`: `view()` is built before the check on whether
there is anything to read. With `stats = []` (E43's accumulate-only use,
where "its value is its state") the only number wanted per row is
`n_eff`, which is one subtraction from the truncated weight. `ew_cov`
pays the clone per row by design when it emits statistics; it should
not for a model that emits nothing. P1's shape, at the cheap end.

**Test.** Bench, as P1's: `k = 400`, `stats = []`, with and without a
`window`. Numbers.

### P4. The window's snapshot ring has no memory budget

`window.rs`, `Snapshots`; `ewridge.rs`, `ewcov.rs`, `ewclass.rs`,
`marginal.rs`, the `offer` calls. Each learned row (or each `window_every`
rows) clones the whole accumulator into the ring: `k²` floats for
`ewridge` and `ew_cov`, `classes · k²` for `ew_class`, `5 · p · T` for
`marginal`. The ring holds a window's worth. `ewridge` at `k = 1 000`
with a 3 600-row window (an hour at 1 Hz) is `8 · 10⁶ · 3 600 ≈ 29 GB`
per instance, times the halflife grid, times the groups; `marginal` at
`p = 6 000, T = 4` and the same window is `0.7 GB` per group. Nothing
checks. `ewridge` refuses a `gram_block_rows` over 256 MiB ("silently
allocating gigabytes is a worse outcome than an error that names the
number") and `marginal` bounds its bins the same way; the ring, which is
larger than either in every realistic configuration, is the one
allocation without a bound. `window_every` is the lever the user has,
and no validator tells them when to pull it.

**Test.** `EwRidgeCfg::validate` with `window / (typical d_clock)` rows
of `k²` floats over a budget: refuse, naming the number and
`window_every`. The typical `d_clock` is not known at validation --
`min_rows` from the bank's clock schedule, or a documented assumption
of one row per clock unit, or a runtime check in `offer` when the ring
first exceeds the budget. Numbers, and a refusal.

### D1. Hard rule 5 vs `MIN_SCHEMA_VERSION = 6`

`lib.rs` documents the 2026-09-07 exception well; `CLAUDE.md` hard rule 5
still reads as unconditional. Put the one-line caveat there too.

### D2. `solve_spd` and `SpdFactor::of` are the same jitter ladder twice

`solve.rs`. Identical today; a future edit to one is a divergence. Write
`solve_spd` as `SpdFactor::of(a, k)` plus a solve.

### D3. `Kalman::coefficients` unscales with the current stats

`kalman.rs`. The betas live in the standardized coordinates of the stats
at the time they were learned; `coefficients()` reads them out with
today's scales and means. The tests acknowledge it ("locking its betas
into the standardized space of the first few rows") and route around it;
the method doc does not say it. `pred` and `coef` are consistent with
each other (both use the current stats), but "coefficient per unit of
`x`" drifts with the standardizer. One paragraph on the method.

### D4. Robust's scale is the plain EW residual variance

`robust.rs`: `s` in the Huber cut and the quantile floor is
`sqrt(sig2)`, the EW mean of squared raw residuals -- which the outliers
Huber exists to resist inflate. The usual choice is a robust scale (MAD
or a Huber-weighted variance). Not a bug; the module doc should say the
scale is not itself robust, since `huber_delta` is "in units of the EW
residual std" and a user will read that as a robust std. T-S4's
`HuberRegressor` (which estimates its scale jointly) is the second
opinion on how much it matters: on `test_river.py`'s 3 %-contaminated
stream the two slopes should be within `0.05`, not the `0.5` that test
allows today.

### D5. `sgd::coefficients` unscales with the current stats, and the step's stats are not quite the scaler's

`sgd.rs`. D3's paragraph applies verbatim: each step is taken in the
standardized coordinates of the moments as they stood on that row, and
`coefficients()`, `predict` and the constraint's projection read the
betas out with today's. The `scale_features` field doc says "unscaling
the coefficients on the way out so they stay in the caller's units" and
should carry D3's caveat. One wrinkle only `sgd` has: the step
standardizes against the moments *with the row admitted at unit weight*
(`including(lam)`) and then updates the scaler at the row's *actual*
weight, so for `weight ≠ 1` the coordinates the gradient was taken in
and the ones the projection and `coefficients()` use a moment later
differ by the row's weight. The doc explains why the prediction does
not depend on the weight; it does not say that the reported
coefficients then do (and that under a constraint the projection is
taken in the second system, not the first). Not a bug at `weight = 1`,
which is every test.

### D6. `window::truncated`'s precision note describes the wrong loss

`window.rs`: "In `f64` it is a difference of positives, so it loses
precision in proportion to the fraction discarded: negligible at `window
= 3·halflife`". True of the weight and the means; for the co-moments the
dominant loss is C17's, which is proportional to `(level/σ)²` and does
not care about the window length. After the fix the note is right as
written; until then it understates the damage by the square of the
offset. `a_windowed_fit_is_the_fit_of_the_rows_inside_the_window`
repeats the note in its tolerance comment ("about eight significant
figures") and would be wrong at any offset.

### V. To confirm on the next pass

- ~~**V1** (C6)~~: closed -- confirmed.
- ~~**V2** (S7)~~: partly closed -- grid duplicates and unknown features
  are refused; feature-set name and within-set duplicates were not found.
- ~~**V3**~~ (C17): closed -- confirmed on re-read: `truncated` clamps `q`
  and not the diagonal of `cen`; `EwCov::var` floors, `cov(i, i)`,
  `comoments()`, `precision()` and `Pca::of` do not go through it. The
  floor belongs on the diagonal after the centred form lands.
- ~~**V4**~~: closed -- `ew_cov` (C14, C15, S15, P3) and `marginal` (C17,
  C18, S17, S18) both read.
- **V5**: `_frame.py` `n_rows` before `predicate` -- the ordering argument
  in the comment is about polars' pushdown; worth one test that pins it,
  since polars' own `pl.defer` does the opposite: `head(100).filter(..)`
  on the plan must return fewer than 100 rows.
- ~~**V6** (C11)~~: closed -- confirmed.
- **V7**: `Kalman::pred_var` reads `self.zs`, a `#[serde(skip)]` scratch
  that a restore zeroes; the first call after a load returns `R` alone.
  Check where the stream reads it. Test: `pred_var` on the first row
  after `ModelBank.load` equals the value the unsaved bank gives.
- ~~**V8** (C13)~~: closed -- the opposite of what `constraint.rs` says.
  `sgd` centres; it is on `lasso`'s side of pattern B, not `ewridge`'s.
- ~~**V9** (C16)~~: closed -- confirmed. `blend_toward_long_run` mixes
  `raw(i, j)` and subtracts `mixed_mean[i] · mixed_mean[j]`; the test
  that pins it uses the same form as its oracle.
- **V10**: `ew_cov`'s `mahal_q` is updated with the row's own score on
  `score.is_finite()` alone, so a zero-weight row's distance enters the
  quantile history; the comment says this follows `resid_quantiles`.
  Check whether `resid_quantiles` admits a zero-weight row's residual
  (`stream.rs`, the scoring block), and pin whichever rule it follows
  for both.
- **V11**: a non-finite feature that is not null. `sgd`'s `dot` gives
  NaN, `NaN.clamp(..)` is NaN, and the coefficients are NaN for good;
  `EwCov::update` folds it into the mean and every co-moment. If
  `plan.accept` (`rows.rs`) rejects `inf` as it rejects null this is
  unreachable; pass 1 read the null case, not this one. Test: Python,
  one `float("inf")` in a feature column mid-stream, then `coef` finite
  on the rows after it -- and the same row counted as skipped in
  `n_skipped`.
- **V12** (C18): whether `MarginalLags` and `EwLagCov` can be snapshotted
  and truncated by the same identity, so `window` + `lags` can be
  allowed in both models rather than refused in both. Read `marglag.rs`
  and `ewlagcov.rs` for how the ring's decay rides on `W_t` (the
  `marglag` module doc says it must not be aged on its own), which is
  the one thing that could make `A(t) − f·A(u)` not apply.
- **V13** (pattern E, breadth): grep every crate for `raw(` and for a
  subtraction of a product of two means -- `- m[i] * m[j]`, `- mean *
  mean`, `- a * b` -- outside `EwCov::raw` itself. `hmm.rs`, `bocpd.rs`,
  `rcov.rs` and `cluster/` all keep covariances and were not read; if
  any of them merges or splits two sets of moments, it is a fifth site.

## What looked right

Worth recording so the next pass does not redo it: the clock's duplicate
value vs backwards value distinction; `reset`, `blend` and `clear_lags`
being applied for skipped rows in `run_instance` (which is what makes S3
the only clock gap); the zero-weight guards in `EwRidge::step`; the
two-pass chunk design in `process_chunk` with the clock committed before
any model moves; the tiled transpose in `rows.rs`; the IO plugin's
projection / predicate / slice order and its `is_pure` reasoning; RLS's
Givens-rotation update, its overflow fallback to `hypot`, and its
zero-pivot handling in back-substitution; Kalman's symmetric `P` update,
its skip of a non-finite innovation, and its once-per-row transition
under `share_p`; `atomic.rs`'s per-process sequence numbering of
temporaries and its symlink resolution; `robust`'s `w_raw` (the
observation count kept apart from the IRLS weights) and its use of the
*prior* residual for the weight; `ew_class`'s per-class empty-window
handling, its factor cache, and its from-scratch oracle test, which is
the best test in the crate and the one to imitate for a windowed check;
`test_river.py`'s two-tier discipline (exact vs statistical, with the
documented ordering difference pinned as its own test), which the
second-opinion file should copy.

From pass 5: `ew_cov::view`'s three-way answer (borrow / truncate /
empty), the shape C2's fix should take; `EwCov`'s blocked Gram merge --
the two-pass centring on the block's own mean, the single copy of
`step_factors` / `commit_scalars` that makes the scalars bit-identical
on both paths, and `a_history_all_but_gone_leaves_nothing_to_cancel`,
which is the numerical argument written as a test; the `debug_assert`
on every matrix read while a block is pending; `EwCovCfg::validate`
refusing `window` with `lags` and with `mahal_quantiles` (the argument
is right, it just stops two short -- C14); `sgd`'s including-the-row
standardization and the `sqrt(n_eff)` bound it buys, with the test that
puts a row a million deviations out and checks the bound is reached;
`sgd`'s two skip arms falling through to one `continue` (pattern C
holds); the fixed-order `DOT_LANES` dot product; `clip_gradient` finite
by default with the Poisson divergence pinned deterministically; the
constraint projection re-run for every target when the scales moved,
not only the targets that stepped.

From pass 6: `marginal::pair`'s per-target empty-window arm (C2's fix in
miniature); `learn`'s handling of a `lam = 0` zero-weight row, which
takes `W_t` to zero deliberately and says why (pattern C holds);
`learn_lags` kept out of `learn`'s loop for a measured vectorization
reason, with the bit-identity to `ew_cov(lags=)` pinned so the split
cannot drift; the three bit-for-bit tests against `EwCov` and
`EwCovModel` (`a_pair_is_the_ew_cov_of_the_two_columns_to_the_bit`,
`lagged_pair_moments_are_ew_covs_to_the_bit`, and the `TargetMoments`
one), which are the in-crate version of what T-S9 does with pandas;
`t_serial_is_standard_where_t_is_over_dispersed`, a Monte Carlo test that
checks the *distribution* of a statistic rather than one value, which
is the right kind of test for a correction; the held-and-replayed bin
warm-up with `learned_edges_lose_no_row` pinning it to the bit; the
window ring's conservative boundary (discard more, never less) and the
`every.max(1)` belt-and-braces that S6 grumbles about; `truncated`
carrying `q` so `n_kish` is windowed too.
